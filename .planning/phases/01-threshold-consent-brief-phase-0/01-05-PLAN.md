---
phase: 01-threshold-consent-brief-phase-0
plan: 05
type: execute
wave: 4
depends_on: [01-04]
files_modified: [docs/PROTOCOL.md, docs/THREAT-MODEL.md, README.md]
autonomous: false
requirements: [CONS-05, CONS-06]
user_setup:
  - service: arc-testnet
    why: "End-of-phase V2 hub deploys (D-12) need a funded deployer key on Arc Testnet"
    env_vars:
      - name: ARC_RPC_URL
        source: ".env (template in .env.example) — https://rpc.testnet.arc.network"
      - name: DEPLOYER_PK
        source: "Existing deployer key; fund with testnet USDC via faucet.circle.com (USDC is the gas token)"

must_haves:
  truths:
    - "docs/PROTOCOL.md has a threshold-consent section with the griefing analysis, stating verbatim: 'worst case is two signature-collection passes: a latency cost, never a safety cost' (CONS-05, D-16)"
    - "The 'No threshold consent' non-goal is superseded — no doc still claims threshold consent doesn't exist"
    - "docs/THREAT-MODEL.md is consistent: refusal ≠ miss (D-07), limitations row updated from 'v2 answer' to shipped"
    - "Fresh V2 hubs (USDC + EURC) are deployed on Arc Testnet with explicit gas discipline; v1 hubs stay live (D-12)"
    - "README documents the V2 hub addresses alongside v1"
  artifacts:
    - path: "docs/PROTOCOL.md"
      provides: "Threshold-consent protocol spec + griefing analysis"
      contains: "a latency cost, never a safety cost"
    - path: "docs/THREAT-MODEL.md"
      provides: "Updated refusal-griefing row and limitations table"
    - path: "README.md"
      provides: "V2 deployed-hub addresses (USDC + EURC) alongside live v1 hubs"
      contains: "ClearingHubV2"
  key_links:
    - from: "docs/PROTOCOL.md griefing section"
      to: "docs/THREAT-MODEL.md row 7 / limitations"
      via: "consistent refusal≠miss terminology and shipped status"
      pattern: "refus"
    - from: "README.md hub table"
      to: "deployed V2 addresses on Arc Testnet"
      via: "arcscan-verifiable contract addresses"
      pattern: "0x"
---

<objective>
Document the threshold-consent protocol and its griefing analysis in `docs/PROTOCOL.md` (D-16, CONS-05), reconcile `docs/THREAT-MODEL.md`, then deploy fresh `ClearingHubV2` hubs for USDC and EURC to Arc Testnet (D-12) and record the addresses — closing the phase with a human verification of the live deployment and dashboard.

Purpose: CONS-05's documentation half and the phase's shipping act. The brief's acceptance bar is the exact sentence: "worst case is two signature-collection passes: a latency cost, never a safety cost."
Output: updated `docs/PROTOCOL.md`, `docs/THREAT-MODEL.md`, `README.md`; two live V2 hubs on Arc Testnet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-threshold-consent-brief-phase-0/01-CONTEXT.md
@.planning/phases/01-threshold-consent-brief-phase-0/01-RESEARCH.md
@.planning/phases/01-threshold-consent-brief-phase-0/01-PATTERNS.md
@docs/V2-BRIEF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PROTOCOL.md threshold-consent section + THREAT-MODEL.md reconciliation</name>
  <files>docs/PROTOCOL.md, docs/THREAT-MODEL.md</files>
  <read_first>
    - docs/PROTOCOL.md (full file — section order Roles → Messages → Netting determinism → Round lifecycle → Settlement semantics → Manifest commitment → Explicit non-goals; the "No threshold consent" bullet at line 127 must be superseded; the netting determinism spec's numbered-rule style is the model)
    - docs/THREAT-MODEL.md (row 7 "grief by refusing" and the limitations table — the new section must stay terminologically consistent)
    - docs/V2-BRIEF.md (§3 Phase 0 — the fixed design language the section must echo)
    - .planning/phases/01-threshold-consent-brief-phase-0/01-RESEARCH.md (section "Griefing analysis content" — the four content bullets are the outline; Pitfall 8 for the consistency rules)
  </read_first>
  <action>
    In docs/PROTOCOL.md add a "Threshold consent" section in Round-lifecycle territory, written in the doc's existing numbered-rule style, covering: (1) the two-pass protocol — propose over the candidate set (everyone with open IOUs), collect consents within a wall-clock window, on timeout exclude ALL non-responders in one deterministic batch, rebuild via filter→re-net→re-propose with the SAME roundNonce, unanimity over the final executed set, hard cap 2 passes, quorum floor ≥2 participants, clean abort on any pass-2 stall; (2) miss semantics — only timeouts count as misses, an explicit reasoned refusal is the safety mechanism working (D-07), re-inclusion is unconditional next round (D-04); (3) the griefing analysis (CONS-05): cost of a stall is one extra collection pass (bounded latency = 2 × window + rebuild compute), repeated stalling costs only repeated latency; safety argument — an excluded member's balance cannot move because they are absent from participants and the contract requires N index-matched signatures; a coordinator that lies about exclusions produces delta mismatches that verifiers refuse; if both passes end up fully signed, each is individually unanimous and the shared roundNonce guarantees at most one executes; coordinator-censorship note — a coordinator can pretend anyone timed out, cost to the victim is latency/lost compression only, mitigation unchanged: anyone can run a coordinator; note that exclusion consumes wall-clock against IOU expiry (Pitfall 5). Include VERBATIM the sentences "worst case is two signature-collection passes: a latency cost, never a safety cost" and "in a payments CCP the defaulter's position is a scalar debit in a stable unit". Remove/rewrite the "No threshold consent — one unresponsive participant stalls the round" non-goal bullet (superseded). In docs/THREAT-MODEL.md update the refusal-griefing row and the limitations table entry from a future "v2 answer" to shipped-in-v2 status, keeping refusal ≠ miss terminology exactly consistent with the new PROTOCOL.md section.
  </action>
  <verify>
    <automated>grep -q 'a latency cost, never a safety cost' docs/PROTOCOL.md && grep -q 'scalar debit in a stable unit' docs/PROTOCOL.md && ! grep -q 'No threshold consent' docs/PROTOCOL.md && grep -qi 'threshold consent' docs/THREAT-MODEL.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/PROTOCOL.md contains both verbatim sentences (exact-string grep passes)
    - The non-goals list no longer claims "No threshold consent" (exact-string grep count 0)
    - The section states, as discrete numbered rules: same-roundNonce rebuild, one-batch exclusion, 2-pass hard cap, ≥2-participant quorum floor, timeout-only miss counting, unconditional re-inclusion
    - docs/THREAT-MODEL.md limitations no longer describe threshold consent as future work, and nowhere calls a reasoned refusal a "miss"
  </acceptance_criteria>
  <done>CONS-05's documentation requirement is met with the brief's exact acceptance phrasing, and both docs tell one consistent story about refusal vs unresponsiveness.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy V2 hubs (USDC + EURC) to Arc Testnet and record addresses</name>
  <files>README.md</files>
  <read_first>
    - contracts/script/DeployV2.s.sol (usage doc-comment with the exact forge invocation from Plan 01-02)
    - README.md (Quickstart + deployed-hubs table — the v1 addresses/format to extend, not replace)
    - .planning/phases/01-threshold-consent-brief-phase-0/01-RESEARCH.md (Code Examples "Deploy + verify (V2, per token)": USDC token 0x3600000000000000000000000000000000000000, EURC token 0x89B50855Aa3bE2F677cD6303Cec089B5F319D72a, --with-gas-price 25gwei, Blockscout verify command with fallback note A1)
  </read_first>
  <action>
    Run the full local gate first: `npm test && npm run test:contracts && npm run e2e:anvil` must be green before any testnet write. Then deploy per RESEARCH Code Examples: forge script contracts/script/DeployV2.s.sol --root contracts --rpc-url "$ARC_RPC_URL" --private-key "$DEPLOYER_PK" --broadcast --with-gas-price 25gwei, once with TOKEN_ADDRESS=0x3600000000000000000000000000000000000000 (USDC) and once with TOKEN_ADDRESS=0x89B50855Aa3bE2F677cD6303Cec089B5F319D72a (EURC). Explicit gas price is mandatory (Arc gas-token gotcha — never let estimation run unbounded). If .env/ARC_RPC_URL/DEPLOYER_PK are missing or the key is unfunded, STOP and create an auth-gate checkpoint asking the user to populate .env (faucet.circle.com for gas USDC) — do not skip the deploy silently. After each deploy, confirm code exists via `cast code <address> --rpc-url "$ARC_RPC_URL"` (non-empty). Attempt Blockscout verification with `forge verify-contract --verifier blockscout --verifier-url https://testnet.arcscan.app/api <address> ClearingHubV2` (RESEARCH assumption A1 — if the verifier API fails, note it for manual verification in the checkpoint and continue). Update README.md's deployed-hubs table with the two ClearingHubV2 addresses labeled Arclear Net v2, keeping the v1 rows live (D-12), and note the HUB_V2_USDC/HUB_V2_EURC env keys. Do NOT write the addresses into .env (gitignored, user-owned) — document them in README only.
  </action>
  <verify>
    <automated>cast code $(grep -oE '0x[a-fA-F0-9]{40}' README.md | tail -2 | head -1) --rpc-url "$ARC_RPC_URL" | grep -qv '^0x$'</automated>
  </verify>
  <acceptance_criteria>
    - Two forge script broadcasts succeeded (one per token) with --with-gas-price 25gwei in the invocation
    - `cast code` returns non-empty bytecode for both new addresses on Arc Testnet
    - README.md deployed-hubs table lists both V2 addresses AND still lists the v1 hubs as live
    - v1 hubs untouched: no transaction was sent to any v1 hub address
  </acceptance_criteria>
  <done>Arclear Net v2 is live on Arc Testnet for USDC and EURC; addresses recorded; v1 remains the live permissionless product (D-12).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify live deployment and the exclusion-round demo</name>
  <files>none (verification only)</files>
  <action>Pause for human verification of the live Arc Testnet deployment and the dashboard exclusion-round walkthrough described in how-to-verify. No file changes; all automation already completed in Tasks 1-2.</action>
  <verify>
    <human-check>User completes the 5-step walkthrough in how-to-verify and responds "approved" or reports issues</human-check>
  </verify>
  <done>User has typed "approved" after confirming arcscan contract pages, the dashboard exclusion-round demo, and the PROTOCOL.md griefing section.</done>
  <what-built>ClearingHubV2 deployed to Arc Testnet for USDC and EURC (addresses in README.md), digest-parity-proven against v1 fixtures, with the two-pass threshold-consent demo running locally: stall toggle, rebuild phases, exclusion-round history, and the e2e liveness scenario all green.</what-built>
  <how-to-verify>
    1. Open https://testnet.arcscan.app and search both V2 addresses from the README table — confirm contract code exists (and source verification status; if Blockscout verification failed in Task 2, verify manually via the explorer UI).
    2. Run `npm run demo -- --anvil`, open http://localhost:4402.
    3. Click Simulate, then toggle one agent's Stall button, then click Run Round — watch the phase line move through collecting-consents → rebuilding → collecting-consents-pass-2, and confirm the round history row shows the exclusion (pass 2, excluded member) with the stalled agent's balance unchanged.
    4. Unstall the agent and run another round — confirm their IOUs settle this time.
    5. Skim the new "Threshold consent" section in docs/PROTOCOL.md — confirm the griefing analysis reads as the phase's acceptance bar ("a latency cost, never a safety cost") and matches what you just watched happen.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe issues (e.g. dashboard display problems, unverified contracts, doc gaps)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy machine → Arc Testnet | Broadcast with DEPLOYER_PK; USDC is both gas token and ERC-20 |
| docs → readers/integrators | Spec claims must match tested behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-19 | Denial of Service | unbounded gas estimation reserving whole USDC balance on deploy | mitigate | --with-gas-price 25gwei mandated in Task 2 invocation (Pitfall 7) |
| T-01-20 | Info Disclosure | DEPLOYER_PK leakage | mitigate | Key read from gitignored .env only; never echoed into README/logs; addresses (public) are the only recorded output |
| T-01-21 | Repudiation | docs claiming untested properties (spec drift vs THREAT-MODEL) | mitigate | Task 1 consistency criteria (Pitfall 8); griefing claims are backed by Plan 01/03 invariant tests and the Plan 04 e2e |
| T-01-22 | Tampering | deploying drifted bytecode to testnet | mitigate | Full local gate (tests + parity + e2e) required green before broadcast |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `grep 'a latency cost, never a safety cost' docs/PROTOCOL.md` matches
- `npm test && npm run test:contracts && npm run e2e:anvil` green before deploy
- Both V2 addresses return non-empty `cast code` on Arc Testnet
- Human checkpoint approved (arcscan + dashboard walkthrough)
</verification>

<success_criteria>
- CONS-05 complete: griefing analysis documented with the brief's exact phrasing, consistent with THREAT-MODEL.md
- CONS-06/D-12 complete: V2 hubs live on Arc Testnet for both tokens, v1 untouched and still live
- Phase success criterion 4 met: repeated refusal costs only rebuild latency — documented and demonstrable on the dashboard
</success_criteria>

<output>
Create `.planning/phases/01-threshold-consent-brief-phase-0/01-05-SUMMARY.md` when done
</output>
