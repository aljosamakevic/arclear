---
phase: 01-threshold-consent-brief-phase-0
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [contracts/src/ClearingHubV2.sol, contracts/test/ClearingHubV2Parity.t.sol, contracts/script/DeployV2.s.sol, src/abi/ClearingHubV2.ts, .env.example]
autonomous: true
requirements: [CONS-06]

must_haves:
  truths:
    - "ClearingHubV2 reproduces the EXISTING test/fixtures/digest.json digest exactly — no fixture regeneration (D-11)"
    - "ClearingHubV2's execution path is byte-identical to v1: same ROUND_TYPEHASH, same EIP712 domain args, same errors/events/checks, withdraw never pausable (D-09)"
    - "Anvil/e2e tooling can deploy V2 bytecode from TypeScript (src/abi/ClearingHubV2.ts exists — Pitfall 2 closed)"
    - "A DeployV2 forge script exists with the Arc gas-price discipline documented in its usage comment"
  artifacts:
    - path: "contracts/src/ClearingHubV2.sol"
      provides: "Near-verbatim v2 settlement contract"
      contains: "contract ClearingHubV2"
      min_lines: 170
    - path: "contracts/test/ClearingHubV2Parity.t.sol"
      provides: "Digest parity vs existing fixture via deployCodeTo"
      contains: "ClearingHubV2.sol:ClearingHubV2"
    - path: "src/abi/ClearingHubV2.ts"
      provides: "clearingHubV2Abi + clearingHubV2Bytecode for anvil deploys"
      exports: ["clearingHubV2Abi", "clearingHubV2Bytecode"]
    - path: "contracts/script/DeployV2.s.sol"
      provides: "TOKEN_ADDRESS-parameterized V2 deploy script"
      contains: "ClearingHubV2"
  key_links:
    - from: "contracts/test/ClearingHubV2Parity.t.sol"
      to: "test/fixtures/digest.json"
      via: "vm.readFile of the SAME v1 fixture (fs_permissions already grant ../test/fixtures)"
      pattern: "test/fixtures/digest\\.json"
    - from: "src/abi/ClearingHubV2.ts"
      to: "contracts/out/ClearingHubV2.sol/ClearingHubV2.json"
      via: "manually maintained abi+bytecode paste after forge build"
      pattern: "clearingHubV2Bytecode"
---

<objective>
Ship `ClearingHubV2.sol` as a near-verbatim copy of `ClearingHub.sol` (D-09: name + NatSpec version marker only), prove D-11 with a digest-parity test against the SAME existing fixture, and produce the deploy artifacts (forge script, TS abi/bytecode module, .env.example keys) that Plans 04/05 need.

Purpose: CONS-06 — V2 ships with the execution path mostly unchanged; the parity test is the machine-checkable proof that no drift occurred.
Output: V2 contract, parity test (Wave 0 gap), `src/abi/ClearingHubV2.ts` (Wave 0 gap), `DeployV2.s.sol`, `.env.example` V2 keys.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-threshold-consent-brief-phase-0/01-CONTEXT.md
@.planning/phases/01-threshold-consent-brief-phase-0/01-RESEARCH.md
@.planning/phases/01-threshold-consent-brief-phase-0/01-PATTERNS.md

<interfaces>
Anchors that MUST survive the copy byte-for-byte (from contracts/src/ClearingHub.sol — read the full file):

```solidity
pragma solidity 0.8.26;
// ROUND_TYPEHASH string:
bytes32 private constant ROUND_TYPEHASH = keccak256(
    "Round(uint64 roundNonce,address[] participants,int256[] deltas,bytes32 manifestHash)"
);
// Constructor domain args (D-11 hinges on this exact pair):
constructor(IERC20 token_) EIP712("ArcClearingHub", "1") Ownable(msg.sender) { token = token_; }
// Never-pausable withdraw signature:
function withdraw(uint256 amount) external nonReentrant {
```

Parity test template (contracts/test/DigestParity.t.sol — mirror exactly, swap names):
```solidity
deployCodeTo("ClearingHub.sol:ClearingHub", abi.encode(address(usdc)), hubAddr);
// V2 variant uses "ClearingHubV2.sol:ClearingHubV2"
import {MockUSDC} from "./utils/RoundBuilder.sol";
```

TS artifact module shape (src/abi/ClearingHub.ts): exactly two exports —
`export const clearingHubAbi = [ ... ]` and `export const clearingHubBytecode = "0x..." as const;`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ClearingHubV2.sol as a near-verbatim copy of ClearingHub.sol</name>
  <files>contracts/src/ClearingHubV2.sol</files>
  <read_first>
    - contracts/src/ClearingHub.sol (the entire 181-line source — the copy source of truth; note the withdraw NatSpec why-comment at lines 81-92 and the contract header block at lines 12-27)
    - .planning/phases/01-threshold-consent-brief-phase-0/01-PATTERNS.md (section "contracts/src/ClearingHubV2.sol" — the copy-verbatim checklist)
  </read_first>
  <action>
    Copy contracts/src/ClearingHub.sol to contracts/src/ClearingHubV2.sol in full, then apply ONLY these diffs (D-09): rename `contract ClearingHub` to `contract ClearingHubV2`, and rewrite the @title/@notice/@dev header block to a v2 marker stating that the threshold-consent protocol (exclude-and-recompute, two-pass consent) lives entirely off-chain in the coordinator/SDK and the execution path is identical to v1 (Arclear Net v2). Keep IDENTICAL: pragma solidity 0.8.26, the OZ import block, EIP712("ArcClearingHub", "1") constructor args, ROUND_TYPEHASH string, every custom error (LengthMismatch, TooFewParticipants, BadSignature(uint256), WrongRoundNonce(uint64,uint64), InsufficientCollateral(address,uint256,uint256), etc. — exactly as in v1), every event, deposit/withdraw/executeRound/hashRound/pause/unpause signatures and bodies, all modifiers. withdraw MUST remain `external nonReentrant` with NO whenNotPaused and its why-comment intact (D-10: no new events; no new external functions). Per-function NatSpec density matches v1 exactly (it survives the copy). Run `cd contracts && forge build` to compile.
  </action>
  <verify>
    <automated>cd contracts && forge build && grep -q 'contract ClearingHubV2 is' src/ClearingHubV2.sol && grep -q 'EIP712("ArcClearingHub", "1")' src/ClearingHubV2.sol && grep -q 'function withdraw(uint256 amount) external nonReentrant {' src/ClearingHubV2.sol && ! grep -v '^\s*///' src/ClearingHubV2.sol | grep 'function withdraw' | grep -q 'whenNotPaused'</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` succeeds and `contracts/out/ClearingHubV2.sol/ClearingHubV2.json` exists
    - `diff contracts/src/ClearingHub.sol contracts/src/ClearingHubV2.sol` shows changes ONLY in the contract-name line and the header NatSpec block (no diff hunks inside executeRound, hashRound, withdraw, deposit, errors, events, or constructor)
    - `grep -c 'Round(uint64 roundNonce,address[] participants,int256[] deltas,bytes32 manifestHash)' contracts/src/ClearingHubV2.sol` outputs 1
    - withdraw carries no whenNotPaused modifier and keeps its "Deliberately NOT pausable" comment
    - No new external functions and no new events vs v1 (event/function counts equal between the two files)
  </acceptance_criteria>
  <done>ClearingHubV2.sol compiles under via_ir with the execution path byte-identical to v1; only the contract name and header NatSpec differ.</done>
</task>

<task type="auto">
  <name>Task 2: Digest-parity test for V2 against the existing fixture</name>
  <files>contracts/test/ClearingHubV2Parity.t.sol</files>
  <read_first>
    - contracts/test/DigestParity.t.sol (the complete 40-line template — fixture JSON parsing, vm.chainId, deployCodeTo at the fixture's pinned hub address, digest + ECDSA.recover assertions, MockUSDC import from ./utils/RoundBuilder.sol)
    - test/fixtures/digest.json (field names the parser reads: .hub, .chainId, .roundNonce, .participants, .deltas, .manifestHash, .digest, .signer0, .consent0)
    - contracts/foundry.toml (fs_permissions already grant read access to ../test/fixtures — no config change needed)
  </read_first>
  <action>
    Create contracts/test/ClearingHubV2Parity.t.sol containing `contract DigestParityV2Test is Test` with one test function test_v2DigestMatchesV1SdkFixture, mirroring DigestParity.t.sol line-for-line with three substitutions: import ClearingHubV2 from ../src/ClearingHubV2.sol, deployCodeTo artifact string "ClearingHubV2.sol:ClearingHubV2" (Pitfall 1: must match the forge out/ layout — forge build ran in Task 1), and the hub variable typed ClearingHubV2. It reads the SAME ../test/fixtures/digest.json (D-11: no regeneration — this is the point of the test). Assert hashRound(...) == expectedDigest with failure message "V2 digest diverges from v1 fixture — D-11 violated" and ECDSA.recover(digest, consent0) == signer0. Import MockUSDC from ./utils/RoundBuilder.sol — do not duplicate it. Do NOT modify RoundBuilder.sol or DigestParity.t.sol.
  </action>
  <verify>
    <automated>cd contracts && forge test --match-contract DigestParityV2 -vvv && forge test --match-contract DigestParityTest -vvv</automated>
  </verify>
  <acceptance_criteria>
    - `cd contracts && forge test --match-contract DigestParityV2 -vvv` passes (1 test, 0 failed)
    - The test file contains the exact artifact string "ClearingHubV2.sol:ClearingHubV2" and reads "../test/fixtures/digest.json"
    - `git status --porcelain test/fixtures/` is empty (fixture untouched — no regeneration)
    - The original DigestParityTest still passes (v1 parity unaffected)
  </acceptance_criteria>
  <done>The unchanged v1 fixture proves V2's domain separator, typehash, and hashRound are byte-compatible — D-11/CONS-06 machine-checked.</done>
</task>

<task type="auto">
  <name>Task 3: V2 deploy script, TS abi/bytecode module, and .env.example keys</name>
  <files>contracts/script/DeployV2.s.sol, src/abi/ClearingHubV2.ts, .env.example</files>
  <read_first>
    - contracts/script/Deploy.s.sol (the complete 21-line template: vm.envAddress("TOKEN_ADDRESS"), startBroadcast/stopBroadcast, console.log of the deployed address, usage doc-comment with --with-gas-price 25gwei)
    - src/abi/ClearingHub.ts (head + tail: the two-export structure — `export const clearingHubAbi = [...]` array literal and final-line `export const clearingHubBytecode = "0x..." as const;`)
    - contracts/out/ClearingHubV2.sol/ClearingHubV2.json (source of truth for the V2 abi array and bytecode.object after Task 1's forge build)
    - .env.example (existing HUB_USDC/HUB_EURC lines 13-14 — V2 keys append alongside, v1 keys stay per D-12)
  </read_first>
  <action>
    (1) Create contracts/script/DeployV2.s.sol: a `contract DeployV2 is Script` mirroring Deploy.s.sol — reads vm.envAddress("TOKEN_ADDRESS"), deploys `new ClearingHubV2(IERC20(token))` inside startBroadcast/stopBroadcast, console.logs "ClearingHubV2 deployed for token %s at %s". The usage doc-comment MUST include the full invocation with `--with-gas-price 25gwei` (Arc gas-token gotcha, Pitfall 7). (2) Create src/abi/ClearingHubV2.ts with exactly two exports: `clearingHubV2Abi` (the abi array pasted from contracts/out/ClearingHubV2.sol/ClearingHubV2.json — content is interface-identical to v1 since no external function/event changed) and `clearingHubV2Bytecode` (the .bytecode.object hex string, `as const`). Generate the file programmatically from the forge out JSON (e.g. a one-off node script reading the JSON and writing the module) rather than hand-pasting, to avoid truncation errors; the committed artifact is the .ts file. (3) Append `HUB_V2_USDC=` and `HUB_V2_EURC=` lines to .env.example with a comment noting v1 hubs stay live as Arclear Net v1 (D-12). Do not modify src/index.ts (abi modules are not barreled — ClearingHub.ts isn't either).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx tsx -e "const v2 = await import('./src/abi/ClearingHubV2.js'); const v1 = await import('./src/abi/ClearingHub.js'); if (v2.clearingHubV2Bytecode === v1.clearingHubBytecode) throw new Error('V2 bytecode identical to v1 — wrong artifact pasted'); if (!v2.clearingHubV2Bytecode.startsWith('0x')) throw new Error('bad bytecode'); const names = (a) => JSON.stringify(a.map((e) => e.name ?? e.type).sort()); if (names(v2.clearingHubV2Abi) !== names(v1.clearingHubAbi)) throw new Error('ABI surface drift'); console.log('abi ok');" && grep -q '^HUB_V2_USDC=' .env.example && grep -q '^HUB_V2_EURC=' .env.example && cd contracts && forge build</automated>
  </verify>
  <acceptance_criteria>
    - src/abi/ClearingHubV2.ts exports exactly clearingHubV2Abi and clearingHubV2Bytecode; bytecode string starts with "0x" and differs from v1's clearingHubBytecode (name/metadata hash differ); ABI entry names match v1's exactly (no interface change)
    - contracts/script/DeployV2.s.sol compiles via forge build and its doc-comment contains "--with-gas-price 25gwei"
    - .env.example contains HUB_V2_USDC= and HUB_V2_EURC= while HUB_USDC=/HUB_EURC= remain present
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All V2 deploy artifacts exist: forge script with Arc gas discipline, TS module ready for anvil deploys (Pitfall 2 closed), env template documents V2 hub addresses alongside live v1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK/relayer → ClearingHubV2 | Signed round submission crosses into the settlement contract |
| copy process → V2 source | Human/agent copy of security-critical v1 code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | ClearingHubV2 copy drift (e.g. accidentally pausable withdraw, altered typehash) | mitigate | Task 1 diff-scoped acceptance + grep gates on withdraw/EIP712/typehash; Task 2 parity test fails on any domain/typehash drift |
| T-01-06 | Spoofing | executeRound signature checks | mitigate | Execution path copied verbatim: per-index ECDSA.recover over the EIP-712 digest, strict-ascending participants, N sigs required (unchanged v1 checks, re-proven by parity + existing revert matrix) |
| T-01-07 | Denial of Service | pause authority | mitigate | withdraw never pausable survives the copy (ASVS V4 control); Ownable2Step unchanged |
| T-01-08 | Tampering | src/abi/ClearingHubV2.ts wrong bytecode (v1 pasted as v2 — Pitfall 2) | mitigate | Task 3 automated check asserts V2 bytecode differs from v1 while ABI surface is identical |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH Package Legitimacy Audit: none this phase) |
</threat_model>

<verification>
- `cd contracts && forge build && forge test -vvv` fully green (v1 suites + new parity test)
- `npx tsc --noEmit` exits 0
- `git status --porcelain test/fixtures/` empty — no fixture regeneration
</verification>

<success_criteria>
- CONS-06/D-09/D-10/D-11: ClearingHubV2 compiles, its execution path is diff-provably identical to v1, no new events/functions, and the EXISTING digest fixture passes against V2 bytecode
- Pitfall 2 closed: TypeScript can deploy genuine V2 bytecode on anvil
- Deploy tooling ready for Plan 05's Arc testnet deploys with explicit gas discipline
</success_criteria>

<output>
Create `.planning/phases/01-threshold-consent-brief-phase-0/01-02-SUMMARY.md` when done
</output>
