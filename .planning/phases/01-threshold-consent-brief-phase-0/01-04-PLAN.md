---
phase: 01-threshold-consent-brief-phase-0
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified: [demo/server.ts, demo/setup.ts, demo/e2e.ts, public/dashboard.html]
autonomous: true
requirements: [CONS-01, CONS-04]

must_haves:
  truths:
    - "The anvil demo/e2e deploys genuine ClearingHubV2 bytecode, not v1 (Pitfall 2 closed end-to-end)"
    - "A per-agent stall toggle is settable via HTTP and visible in /state; stalling is distinguishable from refusal-for-cause (D-13)"
    - "A pass-2 abort returns HTTP 200 with a structured aborted outcome — 500 is reserved for genuine faults (Pitfall 6)"
    - "The dashboard shows rebuild phases, exclusion rounds (excluded members + pass count) in round history, and stall toggles (D-14)"
    - "npm run e2e:anvil proves the canonical liveness scenario: stall → round settles without the staller → their IOUs settle cleanly next round → never twice (D-15, CONS-04)"
  artifacts:
    - path: "demo/setup.ts"
      provides: "V2 bytecode deploy on anvil; HUB_V2_* env reads on testnet"
      contains: "clearingHubV2Bytecode"
    - path: "demo/server.ts"
      provides: "POST /stall toggle + abort-aware /round + enriched /state"
      contains: "/stall"
    - path: "public/dashboard.html"
      provides: "Stall toggles, new phase labels, exclusion-round history columns"
      contains: "collecting-consents-pass-2"
    - path: "demo/e2e.ts"
      provides: "Scripted liveness scenario with balance and manifest assertions"
      contains: "stalled"
  key_links:
    - from: "demo/setup.ts"
      to: "src/abi/ClearingHubV2.ts"
      via: "import of V2 abi + bytecode (Plan 01-02 artifact)"
      pattern: "ClearingHubV2"
    - from: "demo/server.ts /round handler"
      to: "Coordinator.runRound structured outcome"
      via: "200 + JSON for settled AND aborted"
      pattern: "outcome"
    - from: "public/dashboard.html"
      to: "GET /state"
      via: "1500ms poll rendering stall flags, miss counters, exclusion history"
      pattern: "fetch\\(\"/state\"\\)"
---

<objective>
Wire the two-pass protocol into the demo surface: anvil bootstrap deploys V2 bytecode, the HTTP server gains a stall toggle and abort-aware round semantics, the dashboard surfaces rebuild phases and exclusion rounds, and the e2e script proves the canonical liveness scenario (D-15) against real anvil settlement.

Purpose: CONS-04's end-to-end proof and the demo visibility decisions (D-13/D-14) — this is how a reviewer sees the system operate through a member failure.
Output: modified `demo/setup.ts`, `demo/server.ts`, `public/dashboard.html`, `demo/e2e.ts`; green `npm run e2e:anvil`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-threshold-consent-brief-phase-0/01-CONTEXT.md
@.planning/phases/01-threshold-consent-brief-phase-0/01-RESEARCH.md
@.planning/phases/01-threshold-consent-brief-phase-0/01-PATTERNS.md
@.planning/phases/01-threshold-consent-brief-phase-0/01-03-SUMMARY.md

<interfaces>
From Plan 01-02 (src/abi/ClearingHubV2.ts):
```typescript
export const clearingHubV2Abi = [ /* interface-identical to v1 */ ];
export const clearingHubV2Bytecode = "0x..." as const;
```

From Plan 01-03 (demo/coordinator.ts — read for exact names):
```typescript
// Coordinator.runRound returns a structured discriminated result:
//   { outcome: "settled", round: ExecutedRound } | { outcome: "aborted", reason, excluded, passCount }
// ExecutedRound now includes: excluded: string[] (lowercase), passCount: number
// RoundPhase now includes: "rebuilding" | "collecting-consents-pass-2" | "aborted"
// AgentPersona (demo/agents.ts) has mutable `stalled: boolean`
// Coordinator constructor accepts consentWindowMs option (default 30_000)
// Coordinator.state() exposes stall flags + missed counters (bigints as strings, lowercase keys)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Point anvil bootstrap at V2 bytecode; add stall toggle + abort-aware /round to the server</name>
  <files>demo/setup.ts, demo/server.ts</files>
  <read_first>
    - demo/setup.ts (v1 abi import at line 16, deployContract at 97-104, testnet HUB_USDC env-read pattern at 138-141, Arc gas discipline comment at 49-55)
    - demo/server.ts (route-dispatch if-chain at 50-56 and 98-111, /state enrichment spread at 57-71 — note: NO request-body parsing exists; per-request variation travels in the URL)
    - demo/coordinator.ts (structured runRound outcome + state() shape from Plan 03)
    - src/abi/ClearingHubV2.ts (export names)
  </read_first>
  <action>
    In demo/setup.ts: swap the import from "../src/abi/ClearingHub.js" to "../src/abi/ClearingHubV2.js" and use clearingHubV2Abi/clearingHubV2Bytecode in the deployContract call (anvil now runs genuine V2 bytecode). In testnet mode, read process.env.HUB_V2_USDC (throw-if-missing guard, same style as the existing HUB_USDC read) — v1 env keys remain untouched for the v1 product. Keep the existing explicit-gas discipline on every write unchanged. In demo/server.ts: (1) add route `POST /stall?agent=<name>` following the existing if-chain style — parse the agent name from the URL query (new URL(req.url, "http://x").searchParams), find the persona case-insensitively by name, flip persona.stalled, respond 200 with { agent, stalled }; unknown agent → 404 (no JSON body parsing, matching the server's URL-only convention); (2) rework the POST /round handler for the structured outcome: both settled and aborted respond 200 with the JSON result (aborted is expected protocol behavior — Pitfall 6); only thrown errors hit the existing catch → 500; call printReport only for settled outcomes; (3) enrich the /state spread with per-agent stalled flags (from personas) alongside coordinator.state()'s missed counters and round history so the dashboard can render everything.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'ClearingHubV2' demo/setup.ts && grep -q 'HUB_V2_USDC' demo/setup.ts && grep -q '/stall' demo/server.ts && grep -c 'src/abi/ClearingHub.js' demo/setup.ts | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - demo/setup.ts no longer imports from "../src/abi/ClearingHub.js" (grep count 0) and deploys clearingHubV2Bytecode on anvil
    - Testnet mode reads HUB_V2_USDC with a throw-if-missing guard; existing HUB_USDC read for v1 is not repurposed
    - POST /stall?agent=Name toggles persona.stalled and returns the new value; unknown agent returns 404
    - POST /round returns 200 for BOTH settled and aborted outcomes (source shows no throw on aborted); catch block still returns 500 for genuine faults
    - /state JSON includes stalled flags and missed counters
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Anvil demo runs V2 bytecode; stalling any agent is one HTTP call; aborted rounds are structured 200 responses; /state carries everything the dashboard needs.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard — stall toggles, rebuild-phase labels, exclusion-round history</name>
  <files>public/dashboard.html</files>
  <read_first>
    - public/dashboard.html (poll-and-render at 101-161, button-action pattern at 163-167, rounds-table row template at 142-149, phase-label map at 152-159 — every new RoundPhase string needs an entry)
    - demo/server.ts (final /state shape from Task 1)
  </read_first>
  <action>
    Extend public/dashboard.html (zero-framework, template-literal innerHTML — stay in that style): (1) add entries to the phase-label map for "rebuilding" (e.g. rebuild message), "collecting-consents-pass-2" (pass-2 message with phaseDetail), and "aborted" (showing the abort reason from state); (2) in the agent-positions rows render a per-agent stall toggle button that POSTs /stall?agent=<name> (fetch, then let the next poll refresh state) and a visual stalled indicator plus the agent's missed-window counter; (3) extend the rounds-table rows with exclusion info: when a round has passCount 2 / non-empty excluded, show pass count and excluded member short-addresses (or names) so exclusion rounds are visibly distinct in history (D-14); keep bigint decoding via BigInt(...) and the existing display-only percent trick. No build step, no external assets, no framework.
  </action>
  <verify>
    <automated>grep -q 'collecting-consents-pass-2' public/dashboard.html && grep -q 'rebuilding' public/dashboard.html && grep -q 'aborted' public/dashboard.html && grep -q '/stall' public/dashboard.html && grep -q 'passCount' public/dashboard.html</automated>
  </verify>
  <acceptance_criteria>
    - Phase-label map contains keys "rebuilding", "collecting-consents-pass-2", "aborted" (source assertion)
    - Each agent row renders a stall toggle wired to POST /stall?agent=<name> and shows stalled state + missed counter from /state
    - Rounds table rows render excluded members and pass count for exclusion rounds
    - File remains a single static HTML file with no new external dependencies (no <script src= additions)
  </acceptance_criteria>
  <done>The dashboard tells the phase's story visually: toggle an agent stalled, watch rebuild phases, see the exclusion round in history (D-13/D-14 demo surface complete).</done>
</task>

<task type="auto">
  <name>Task 3: E2E liveness scenario — stall, rebuild-settle, next-round re-settlement, never twice</name>
  <files>demo/e2e.ts</files>
  <read_first>
    - demo/e2e.ts (full 74-line script: before-snapshot/assert pattern at 29-58, [e2e] log prefix, anvil kill + process.exit at 73-74)
    - demo/coordinator.ts (structured runRound result; how to reach persona.stalled and consentWindowMs)
    - .planning/phases/01-threshold-consent-brief-phase-0/01-RESEARCH.md (Pitfall 5: excluded IOUs must have expiries comfortably beyond both rounds; Pitfall 3: process.exit masks leaked timers — still clear/unref them)
  </read_first>
  <action>
    Extend demo/e2e.ts with the D-15 canonical scenario after the existing baseline round assertions: (1) generate fresh IOUs ensuring at least one persona (the designated staller) has IOUs with far-future expiries (Pitfall 5); (2) set staller.persona.stalled = true and use a short consentWindowMs override (e.g. 2000ms for e2e) so the run stays fast; (3) run round n — assert outcome is settled with passCount 2, the staller's address is in excluded, the staller's collateral delta is exactly 0n, and none of the staller's IOU ids appear in round n's consumedIds; (4) unstall (persona.stalled = false) and run round n+1 — assert settled, the previously excluded IOU ids ARE in round n+1's consumedIds, per-agent collateral deltas match the engine output to the base unit (existing before/after snapshot pattern), and consumedIds of rounds n and n+1 are disjoint (never settles twice, CONS-04); (5) count mismatches and process.exit(1) on any failure, [e2e] log prefix throughout. The script runs against anvil which now deploys V2 bytecode (Task 1) — assert the deployed hub bytecode differs from v1 (e.g. compare the tail of eth_getCode against clearingHubV2Bytecode's suffix, or at minimum log the code hash) so the "silently exercising v1" failure mode (Pitfall 2) is caught here.
  </action>
  <verify>
    <automated>npm run e2e:anvil</automated>
  </verify>
  <acceptance_criteria>
    - `npm run e2e:anvil` exits 0 with the liveness scenario assertions all logged ✓
    - Scenario asserts, in order: staller excluded with delta 0n in round n; staller's ids absent from manifest n; ids present in manifest n+1; manifest disjointness across n and n+1
    - Round n reports passCount 2 and non-empty excluded in its ExecutedRound record
    - Script terminates without hanging (timers cleared/unref'd before exit)
  </acceptance_criteria>
  <done>The phase's success criterion 1 and 3 are proven end-to-end against real anvil settlement: a stalled member cannot block the round, their paper settles next round, and nothing settles twice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → demo server | /stall and /round are unauthenticated demo endpoints |
| demo → anvil/testnet | Chain writes with real gas semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Denial of Service | POST /stall (anyone can stall any agent) | accept | Demo-only, non-custodial control surface (RESEARCH ASVS V5 note); stalling triggers exclusion, never fund movement — the protocol's whole point |
| T-01-16 | Tampering | wrong bytecode exercised (v1 masquerading as V2) | mitigate | Task 1 import swap + Task 3 e2e bytecode assertion (Pitfall 2) |
| T-01-17 | Info Disclosure | /state exposes miss counters and stall flags | accept | Public-by-design demo telemetry; no keys, no secrets (coordinator holds no keys) |
| T-01-18 | Denial of Service | unbounded gas estimation on Arc writes | mitigate | Existing explicit-gas discipline preserved (maxFeePerGas + gas on every write; Pitfall 7) |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `npm run e2e:anvil` exits 0 (liveness scenario green)
- `npm test && npm run test:contracts` green (wave gate — no regression from demo changes)
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- CONS-01 demo surface: stall toggle injects unresponsiveness distinguishable from refusal (D-13)
- CONS-04 proven end-to-end on anvil with V2 bytecode: exclusion round settles, excluded IOUs settle next round, never twice (D-15)
- D-14 visibility: rebuild phases, exclusion history, and miss counters all rendered by the dashboard
- Pitfall 6: aborts are 200-structured outcomes, not 500s
</success_criteria>

<output>
Create `.planning/phases/01-threshold-consent-brief-phase-0/01-04-SUMMARY.md` when done
</output>
