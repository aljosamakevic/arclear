---
phase: 01-threshold-consent-brief-phase-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/round.ts, test/rebuild.test.ts]
autonomous: true
requirements: [CONS-02, CONS-03, CONS-05]

must_haves:
  truths:
    - "Rebuilding a round over any IOU set minus any excluded-member subset produces deltas that sum to exactly 0n (CONS-05)"
    - "No excluded address ever appears in a rebuilt proposal's participants, and no consumed id in the rebuilt manifest belongs to an IOU touching an excluded member (CONS-02)"
    - "A participant given the excluded list can independently re-verify a rebuilt proposal from their own IOU view (CONS-03 client half)"
    - "A coordinator that lies about the excluded set (excluded address still in participants, or withheld exclusion causing delta mismatch) is refused with a diagnostic reason, never accepted"
  artifacts:
    - path: "src/round.ts"
      provides: "rebuildProposal export + verifyProposal excluded-aware verification"
      exports: ["rebuildProposal", "verifyProposal"]
      contains: "export function rebuildProposal"
    - path: "test/rebuild.test.ts"
      provides: "fast-check property tests for rebuild zero-sum, exclusion completeness, determinism, and excluded-aware verification"
      min_lines: 120
  key_links:
    - from: "src/round.ts rebuildProposal"
      to: "src/netting.ts net()"
      via: "filter openIous then re-net (no new netting math)"
      pattern: "net\\(kept"
    - from: "src/round.ts rebuildProposal"
      to: "buildProposal"
      via: "terminates in the existing fixture-locked digest path"
      pattern: "buildProposal\\("
---

<objective>
Implement the pure exclude-and-recompute core in the SDK: `rebuildProposal` (filter IOUs touching excluded members → re-net → re-propose with the SAME roundNonce) and an `excluded`-aware extension of `verifyProposal`, plus the fast-check property tests that pin CONS-02/CONS-03/CONS-05 invariants.

Purpose: This is the protocol change of the whole phase (CONS-06 says the change lives in SDK/coordinator, not the contract). Every downstream plan (coordinator state machine, e2e) composes these two functions.
Output: `src/round.ts` with `rebuildProposal` + extended `verifyProposal`; new `test/rebuild.test.ts` (Wave 0 gap from 01-VALIDATION.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-threshold-consent-brief-phase-0/01-CONTEXT.md
@.planning/phases/01-threshold-consent-brief-phase-0/01-RESEARCH.md
@.planning/phases/01-threshold-consent-brief-phase-0/01-PATTERNS.md

<interfaces>
Existing contracts the executor builds against (from src/round.ts, src/netting.ts, src/types.ts — read them for full bodies):

```typescript
// src/netting.ts — fixture-locked pure engine, DO NOT MODIFY
export function net(
  ious: SignedIou[],
  opts: { now: bigint; safetyWindowSeconds?: bigint; settledIds?: ReadonlySet<Hex> },
): NetResult;

// src/round.ts — existing exports
export function buildProposal(hub: Address, roundNonce: bigint, result: NetResult, chainId?: number): RoundProposal;
export function verifyProposal(
  hub: Address, proposal: RoundProposal, myIous: SignedIou[], self: Address,
  opts: { now: bigint; safetyWindowSeconds?: bigint; settledIds?: ReadonlySet<Hex>; chainId?: number },
): { ok: boolean; reason?: string };
export function signConsent(hub: Address, proposal: RoundProposal, account: Account, chainId?: number): Promise<Hex>;
export function verifyConsent(hub: Address, proposal: RoundProposal, signer: Address, signature: Hex, chainId?: number): Promise<boolean>;

// src/types.ts — RoundProposal must NOT be widened (mirrors the signed struct; D-08)
export interface RoundProposal {
  roundNonce: bigint; participants: Address[]; deltas: bigint[];
  manifestHash: Hex; digest: Hex; consumedIds: Hex[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement rebuildProposal with zero-sum / exclusion-completeness / determinism property tests</name>
  <files>src/round.ts, test/rebuild.test.ts</files>
  <read_first>
    - src/round.ts (current buildProposal/verifyProposal/manifestHash/roundDigest — rebuildProposal must terminate in the existing buildProposal call, never hand-assemble a digest)
    - src/netting.ts (net() signature, opts shape, numbered-rules doc-comment style, lowercase-key address convention at lines 33-34 and 45-50)
    - test/netting.test.ts (fakeIou helper, NOW/FUTURE constants, ADDRS pool, arbIou/arbIous arbitraries, property-assertion style — reuse these patterns verbatim)
    - .planning/phases/01-threshold-consent-brief-phase-0/01-PATTERNS.md (section "src/round.ts — rebuildProposal" for the exact filter pattern and doc-comment requirements)
  </read_first>
  <behavior>
    - Test "rebuild deltas always sum to zero": for arbitrary ious (arbIous) and arbitrary excluded subset (fc.subarray(ADDRS, { maxLength: ADDRS.length - 2 })), rebuildProposal(HUB, 0n, ious, excluded, { now: NOW }).result.deltas reduces to 0n (CONS-05)
    - Test "no excluded address in rebuilt participants": every participant in result.participants, lowercased, is absent from the lowercased excluded set (CONS-02)
    - Test "no consumed id touches an excluded member": map each consumed id back to its locally-constructed fakeIou; assert neither debtor nor creditor is excluded (CONS-02)
    - Test "rebuild is shuffle-deterministic": rebuildProposal over a shuffled copy of ious equals the unshuffled result (same participants, deltas, manifestHash, digest) — mirrors the existing shuffle-determinism property in test/netting.test.ts
    - Test "cascade removal": a member whose only IOUs touch an excluded member drops out of the rebuilt participant set (rule 6 behavior — RESEARCH Pattern 1 cascade note)
    - Test "settledIds still filter": an id present in opts.settledIds never appears in rebuilt consumedIds
  </behavior>
  <action>
    In src/round.ts add export function rebuildProposal(hub: Address, roundNonce: bigint, openIous: SignedIou[], excluded: Address[], opts: { now: bigint; safetyWindowSeconds?: bigint; settledIds?: ReadonlySet<Hex>; chainId?: number }): { proposal: RoundProposal; result: NetResult }. Implementation per RESEARCH.md Pattern 1: build const ex = new Set(excluded.map(a => a.toLowerCase())); keep each SignedIou iff neither iou.debtor.toLowerCase() nor iou.creditor.toLowerCase() is in ex; call the unchanged net(kept, opts); return { proposal: buildProposal(hub, roundNonce, result, opts.chainId), result }. Do NOT add any new netting math and do NOT touch net() or the digest path. Positional params first, inline-typed opts last, trailing chainId inside opts — matches the existing verifyProposal signature convention. Add a /** ... */ doc comment (prose, 1-3 lines) stating: pure function; reuses the SAME roundNonce as pass 1; the excluded list is out-of-band coordinator metadata that is never part of the signed Round struct (D-08); no division anywhere. Do NOT modify src/types.ts (RoundProposal stays an exact mirror of the signed struct per RESEARCH open question 1) and do NOT touch src/index.ts (round.ts is already in the flat barrel). Create test/rebuild.test.ts implementing the behavior list above: copy the NOW/FUTURE/ADDRS/fakeIou/arbIou/arbIous scaffolding from test/netting.test.ts (fakeIou builds ids via keccak256(toHex(...)) so ids map back to parties), add const arbStalled = fc.subarray(ADDRS, { maxLength: ADDRS.length - 2 }), and a fixed HUB address constant "0x1111111111111111111111111111111111111111". Write the tests FIRST, confirm they fail to compile/run (rebuildProposal missing), then implement.
  </action>
  <verify>
    <automated>npx vitest run test/rebuild.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function rebuildProposal" src/round.ts` outputs 1
    - src/round.ts contains no new import beyond what already exists (rebuild composes net + buildProposal already imported/defined in the module)
    - `npx vitest run test/rebuild.test.ts` passes with at least 6 test cases and fast-check default 100 runs (no fc.assert numRuns override lowering coverage)
    - `npx tsc --noEmit` exits 0
    - git diff shows zero changes to src/netting.ts, src/types.ts, src/index.ts
  </acceptance_criteria>
  <done>rebuildProposal exists as a pure filter→net→buildProposal composition; all six property/unit tests pass; strict tsc green; no changes leaked into the netting engine or type contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend verifyProposal with excluded-aware recomputation and lying-coordinator refusal tests</name>
  <files>src/round.ts, test/rebuild.test.ts</files>
  <read_first>
    - src/round.ts (verifyProposal body — the existing check sequence: self-in-participants, delta recompute/compare, consumedIds vs settledIds, manifestHash recompute, digest recompute; every new check is one early-return in the same { ok, reason } style)
    - test/eip712.test.ts (real-signature test pattern: privateKeyToAccount with repeated-byte keys, signConsent/verifyConsent round trip, negative-case reason-regex assertions at lines 83-94)
    - .planning/phases/01-threshold-consent-brief-phase-0/01-PATTERNS.md (validation-return pattern excerpt — reasons interpolate the failing values)
  </read_first>
  <behavior>
    - Test "honest rebuild verifies": build pass-1 proposal over ious for 3 signers, exclude one, rebuildProposal, then verifyProposal with opts.excluded set for each remaining participant using their full local IOU view returns { ok: true }
    - Test "self excluded → refuse": verifyProposal where self appears in opts.excluded returns ok: false with reason matching /excluded/
    - Test "excluded address in participants → refuse": tamper a rebuilt proposal so an excluded address is present in participants (recompute manifestHash+digest so only this check can fire); verifyProposal returns ok: false with reason matching /excluded/
    - Test "withheld exclusion → delta mismatch refusal": verify a rebuilt proposal WITHOUT passing opts.excluded (participant not told who was dropped) — local net() over unfiltered IOUs disagrees, returns ok: false with reason matching /delta mismatch/
    - Test "consent signatures bind to the rebuilt digest": signConsent over the rebuilt proposal verifies via verifyConsent for the signer and fails for another account (pass-1 signatures can never be replayed on the pass-2 digest)
    - Regression: `npm test` — all existing eip712/netting tests still pass with the widened opts type
  </behavior>
  <action>
    Widen verifyProposal's opts with excluded?: Address[] (optional — omitted means pass-1 semantics, zero behavior change for existing callers). Add three excluded-aware behaviors, each an early-return { ok: false, reason } with the failing address interpolated: (1) if self.toLowerCase() is in the lowercased excluded set, refuse with a reason containing "excluded"; (2) if any proposal.participants entry, lowercased, is in the excluded set, refuse naming the offending address; (3) before the existing net(myIous, opts) recomputation, filter myIous with the same drop-every-IOU-touching-an-excluded-member predicate used by rebuildProposal (extract a small module-private helper, e.g. an unexported filterExcluded(ious, excluded), used by both rebuildProposal and verifyProposal so the two paths can never diverge). All other existing checks (settledIds, manifestHash, digest) remain untouched and still run. Never throw — { ok, reason } only. Update the verifyProposal doc comment to note the excluded list is out-of-band metadata folded into the local recomputation (zero-trust: a coordinator lie produces a delta mismatch or an explicit exclusion violation). Append the behavior-list tests to test/rebuild.test.ts using the eip712.test.ts real-account pattern (three accounts from "0x11"*32, "0x22"*32, "0x33"*32 keys; IOUs between their addresses need real ids — use signIou or the fixed-digest fakeIou pattern consistently with what verifyProposal recomputes). Write tests first, watch the new ones fail, then implement.
  </action>
  <verify>
    <automated>npx vitest run test/rebuild.test.ts && npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - verifyProposal's opts type includes `excluded?: Address[]` (grep "excluded?: Address[]" src/round.ts matches)
    - A single shared module-private exclusion-filter helper is used by both rebuildProposal and verifyProposal (grep for the helper name appears at least 3 times in src/round.ts: 1 definition + 2 call sites)
    - `npx vitest run test/rebuild.test.ts` passes including all Task 2 cases; refusal tests assert both `check.ok === false` and a reason regex
    - `npm test` exits 0 (eip712 + netting suites unaffected)
    - verifyProposal still never throws: no `throw` statement inside the verifyProposal function body
  </acceptance_criteria>
  <done>verifyProposal accepts honest rebuilt proposals given the excluded list, refuses every modeled coordinator lie with a diagnostic reason, existing suites are green, and both rebuild and verify share one exclusion filter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinator → participant | Rebuilt proposal + out-of-band excluded list are untrusted input to verifyProposal |
| participant → contract | Consent signatures over the rebuilt digest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | verifyProposal (coordinator lies about excluded set) | mitigate | Task 2: local re-filter + re-net produces delta mismatch; explicit excluded-in-participants and self-excluded refusals with reasons |
| T-01-02 | Spoofing | pass-2 digest (replay pass-1 signatures) | mitigate | Task 2 test: consent binds to the rebuilt digest via verifyConsent; different deltas/manifest → different digest; on-chain BadSignature backstop unchanged |
| T-01-03 | Tampering | rebuildProposal (double-settle excluded IOUs) | mitigate | settledIds filter preserved through the rebuild path (Task 1 test: settled id never in rebuilt consumedIds) |
| T-01-04 | Info Disclosure | excluded list metadata | accept | Excluded list is public protocol metadata by design; no PII, no secret |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH Package Legitimacy Audit: none this phase) |
</threat_model>

<verification>
- `npx vitest run test/rebuild.test.ts` green (all Task 1 + Task 2 cases)
- `npm test` green (regression: eip712 + netting)
- `npx tsc --noEmit` exits 0
- `git diff --stat` touches only src/round.ts and test/rebuild.test.ts
</verification>

<success_criteria>
- CONS-02: rebuild drops every IOU touching an excluded member and recomputes via the unchanged net(); proven by property tests
- CONS-05 (zero-sum half): rebuilt deltas sum to 0n for arbitrary IOU sets and excluded subsets
- CONS-03 (client half): a participant can independently verify any rebuilt proposal and refuses every modeled coordinator lie
- Digest/struct/fixtures untouched (D-08, D-11): no change to types.ts, domain.ts, netting.ts, or fixtures
</success_criteria>

<output>
Create `.planning/phases/01-threshold-consent-brief-phase-0/01-01-SUMMARY.md` when done
</output>
