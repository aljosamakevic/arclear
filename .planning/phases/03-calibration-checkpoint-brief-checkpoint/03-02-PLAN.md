---
phase: 03-calibration-checkpoint-brief-checkpoint
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - test/thresholdCrossValidation.test.ts
  - demo/thresholdSweep.ts
  - package.json
  - docs/sweep/threshold-sweep.csv
autonomous: true
requirements: [CALB-01]

must_haves:
  truths:
    - "A vitest harness drives the REAL Coordinator attemptRound (injected providers, real EIP-712 consent signatures, in-memory submit stub) over the same seeded flows and availability draws as the model, and per-round outcomes match the model EXACTLY (bigint equality) — the faithfulness proof (D-02)"
    - "npm run sweep:threshold completes end-to-end and writes docs/sweep/threshold-sweep.csv covering the full D-03 grid: n {5,10,15,30,50} x p {1.0,0.97,0.95,0.9,0.8} x the 20 v1 (density,reciprocity) combos, 200 seeds per cell"
    - "Every D-04 metric is a CSV column: realized compression, worst-participant saving (median+p10), excluded-paper latency (mean+p95), abort rate, fraction of rounds settling with exclusions"
    - "The console prints the D-05 headline table: realized compression and p10 saving at n=15/30/50 for p=0.95 and p=0.9 next to the idealized p=1.0 numbers"
  artifacts:
    - path: "test/thresholdCrossValidation.test.ts"
      provides: "Model-vs-real-attemptRound exact-match cross-validation over cells n in {5,15} x p in {1.0,0.9}, 10 seeds each"
      min_lines: 100
    - path: "demo/thresholdSweep.ts"
      provides: "Grid driver over simulateThresholdHistory with CSV emission and headline console table"
      min_lines: 100
    - path: "docs/sweep/threshold-sweep.csv"
      provides: "Full-grid committed calibration data: 500 data rows + 1 header row"
      contains: "n,p,density,reciprocity"
  key_links:
    - from: "test/thresholdCrossValidation.test.ts"
      to: "demo/coordinator.ts"
      via: "imports and drives the real attemptRound"
      pattern: "attemptRound"
    - from: "test/thresholdCrossValidation.test.ts"
      to: "demo/thresholdModel.ts"
      via: "replays availabilityUniforms + deriveRoundSeed for identical inputs"
      pattern: "availabilityUniforms"
    - from: "demo/thresholdSweep.ts"
      to: "demo/thresholdModel.ts"
      via: "simulateThresholdHistory per cell/seed"
      pattern: "simulateThresholdHistory"
    - from: "package.json"
      to: "demo/thresholdSweep.ts"
      via: "sweep:threshold npm script"
      pattern: "sweep:threshold"
---

<objective>
Prove the 03-01 model faithful to the real protocol (D-02 cross-validation against `attemptRound` — without this the headline number is a model artifact), then run the full D-03 grid and emit the CALB-01 deliverable: `docs/sweep/threshold-sweep.csv` plus the D-05 headline table answering "what member count does threshold consent actually unlock under realistic uptime".

Purpose: CALB-01 — the submission slide's empirical answer, with realized (post-exclusion) compression rather than idealized numbers.
Output: cross-validation test, `demo/thresholdSweep.ts`, `sweep:threshold` npm script, committed `docs/sweep/threshold-sweep.csv`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-calibration-checkpoint-brief-checkpoint/03-CONTEXT.md
@demo/thresholdModel.ts
@demo/coordinator.ts
@demo/flowModel.ts
@demo/sweep.ts
@src/round.ts
@docs/sweep/sweep.csv

<interfaces>
<!-- Contracts consumed from plan 03-01 (demo/thresholdModel.ts) — use directly, no exploration needed. -->

From demo/thresholdModel.ts:
- simulateThresholdHistory(params: ThresholdParams): ThresholdHistory
- ThresholdParams: { n, density, reciprocity, seed, rounds, uptime: number | number[], maxIousPerEdge? }
- ThresholdRoundRecord: { kind: "settled-1pass" | "settled-2pass" | "aborted" | "empty", excluded: string[], settledVolume: bigint, grossVolume: bigint, consumedCount: number, deltas: Map<string, bigint>, outflows: Map<string, bigint> } — outflows is lowercase debtor -> gross outflow over the round's CONSUMED set (frozen 03-01 contract; feeds the worst-participant-saving metric); deltas and outflows are empty Maps for aborted/empty rounds
- ThresholdHistory: { records, excludedLatencies: number[], generatedCount, unsettledCount }
- availabilityUniforms(seed, rounds, n): Float64Array — layout [r*n*2 + i*2 + (pass-1)]; member i online in pass k of round r iff value < p_r (strict less-than)
- deriveRoundSeed(seed, round): number — seeds round r's fresh flow batch
- roundFlowBatch(seed, round, { n, density, reciprocity, maxIousPerEdge? }): SignedIou[] — generateFlows wrapped with round-unique re-iding (round index folded into the high 8 nibbles of each id). The harness MUST source batches from this helper, NEVER from generateFlows directly: generateFlows resets its id counter per call, so raw batches reuse ids across rounds — model and harness would corrupt identically and the exact-match assertion would pass while hiding the bug

From demo/coordinator.ts:
- attemptRound(args: { hub, roundNonce, openIous, settledIds, redeemedIds?, providers: Map<string, ConsentProvider>, windowMs, now, chainId?, submit, onPhase? }): Promise<RoundAttemptOutcome>
- ConsentProvider = (proposal: RoundProposal, excluded: Address[]) => Promise<ConsentOutcome>; ConsentOutcome = { kind: "consent", signature } | { kind: "refusal", reason }
- RoundAttemptOutcome: { outcome: "settled", proposal, result, passCount: 1|2, excluded, ... } | { outcome: "aborted", reason, excluded, passCount, ... } | { outcome: "empty", reason }

From src/round.ts:
- signConsent(hub: Address, proposal: RoundProposal, account, chainId?): Promise<Hex> — real EIP-712 consent signature (screenConsents inside attemptRound verifies it; garbage signatures get demoted to refusals, so signatures MUST be real)

From demo/flowModel.ts:
- generateFlows({ n, density, reciprocity, seed, maxIousPerEdge? }): SignedIou[] — participant i's synthetic address is hex(i+1) left-padded to 40 nibbles
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-validation harness — model vs real attemptRound, exact match (D-02)</name>
  <files>test/thresholdCrossValidation.test.ts</files>
  <read_first>demo/coordinator.ts (attemptRound + screenConsents + collectConsents — provider timeout semantics), demo/thresholdModel.ts (shared draw exports), src/round.ts (signConsent signature), test/eip712.test.ts (existing viem account construction style)</read_first>
  <action>
    Create test/thresholdCrossValidation.test.ts. Cells: n in {5, 15} x p in {1.0, 0.9}, reciprocity 0.8, density 0.5, 10 seeds per cell, 5 rounds per seed (D-02's subset). For each (cell, seed):
    1. Model side: simulateThresholdHistory({ n, density: 0.5, reciprocity: 0.8, seed, rounds: 5, uptime: p }).
    2. Real side: build n deterministic viem accounts via privateKeyToAccount from viem/accounts (private key = hex(i+1) left-padded to 64 nibbles). Build a remap from flowModel's synthetic address of member i to accounts[i].address. Per round r: generate the SAME batch via roundFlowBatch(seed, r, { n, density: 0.5, reciprocity: 0.8 }) — the round-unique-id helper; never call generateFlows directly here, colliding ids would corrupt model and harness identically and exact-match would hide it — then remap every IOU's debtor/creditor through the member map (netting never checks IOU signatures and ids are untouched by the remap, so equality with the model is preserved), append to an open pool.
    3. Providers: one per account, keyed by lowercase address. On call, determine pass from the excluded argument (empty array = pass 1, non-empty = pass 2), look up availabilityUniforms(seed, 5, n) at [r*n*2 + i*2 + (pass-1)]: if >= p return a never-resolving Promise (stall -> coordinator timeout), else return { kind: "consent", signature: await signConsent(hub, proposal, accounts[i], undefined) }. Signatures MUST be real — screenConsents demotes invalid ones to refusals, which would silently diverge from the model.
    4. Drive attemptRound({ hub: a fixed dummy address constant, roundNonce: BigInt(r), openIous: openPool filtered by settledIds (or pass settledIds and let net() filter — match the model's call shape), settledIds, providers, windowMs: 500, now: 1_800_000_000n, submit: async stub returning a constant 32-byte hex }). On outcome "settled", fold proposal.consumedIds into settledIds (lowercase), exactly as Coordinator.runRound does.
    Also assert, for every settled or aborted outcome, that outcome.pass1.refused is EMPTY — an offline member must always manifest as a timeout, so any refusal means consent signing or verification broke (a diagnosable failure, not a tolerance case).
    5. Assert per round, model record vs real outcome: kind mapping ("settled-1pass" <-> outcome settled passCount 1; "settled-2pass" <-> settled passCount 2; "aborted" <-> aborted; "empty" <-> empty), excluded.length equal, and for settled rounds EXACT bigint equality of result.settledVolume, result.grossVolume, and consumedIds.length vs consumedCount. Also assert cumulative settled volume over the 5 rounds is bigint-equal.
    Tolerance criterion (explicit, for CALIBRATION.md methodology): because both sides consume identical seeded flows and identical availability uniforms, the acceptance tolerance is EXACT equality (relative difference 0). If any assertion requires loosening, that is a model-fidelity bug to fix in demo/thresholdModel.ts, not a tolerance to widen.
    Set this file's vitest timeout to 240_000 (stalled providers burn the full 500ms window per affected pass). Keep the file self-contained; no changes to demo/coordinator.ts or demo/thresholdModel.ts.
  </action>
  <verify>
    <automated>npx vitest run test/thresholdCrossValidation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 cells x 10 seeds pass with EXACT bigint equality on per-round settledVolume, grossVolume, consumedCount and cumulative settled volume (no epsilon anywhere: grep -c "toBeCloseTo" test/thresholdCrossValidation.test.ts == 0)
    - The p=0.9 cells exercise at least one "settled-2pass" and the suite asserts at least one exclusion occurred across them (guard against a vacuous all-online run)
    - File imports attemptRound from ../demo/coordinator.js and signConsent from ../src/round.js (real protocol path, not a reimplementation)
    - The suite asserts outcome.pass1.refused is empty on every settled/aborted outcome (grep -c "refused" test/thresholdCrossValidation.test.ts >= 1)
    - npx vitest run test/thresholdCrossValidation.test.ts completes in under 3 minutes locally
  </acceptance_criteria>
  <done>The model's numbers are proven to be the real protocol's numbers on the D-02 subset — exact match, no tolerance.</done>
</task>

<task type="auto">
  <name>Task 2: Threshold sweep driver, full D-03 grid, CSV + headline table</name>
  <files>demo/thresholdSweep.ts, package.json, docs/sweep/threshold-sweep.csv</files>
  <read_first>demo/sweep.ts (cell/percentile/CSV emission pattern and seed-mixing style to replicate), demo/thresholdModel.ts (history fields feeding each metric), docs/sweep/sweep.csv (v1 baseline the headline table contrasts against)</read_first>
  <action>
    Create demo/thresholdSweep.ts modeled on demo/sweep.ts's structure (D-09: sibling script, existing sweep untouched for v1 reproducibility). Grid per D-03: N_GRID = [5, 10, 15, 30, 50]; P_GRID = [1.0, 0.97, 0.95, 0.9, 0.8]; FLOW_COMBOS = the union of v1's (density, reciprocity) combos — (0.5, r) for r in 0.0..1.0 step 0.1 (11 combos) plus (d, 0.8) for d in 0.1..1.0 step 0.1 excluding the duplicate (0.5, 0.8) (9 more) = 20 combos. SEEDS = 200 (D-03, matching v1 methodology), ROUNDS_PER_SEED = 10 (const, overridable via a --rounds CLI flag parsed from process.argv; a --quick flag may reduce SEEDS for dev runs but the committed CSV must be the full 200-seed run). Seed mixing mirrors v1's style (seed * 7919 + n * 104729 + reciprocity term) so cells are decorrelated.
    Per cell aggregate over seeds (statistical division is fine here — reporting code, per existing sweep style):
    - realized compression per seed = 1 - Number(sum settledVolume) / Number(sum grossVolume) over settled records only (post-exclusion realized number, D-04); aggregate median + p10 across seeds
    - worst-participant saving per seed = minimum across settled records of the per-round v1 metric (for each net debtor in record.deltas: 1 - Number(netDebit) / Number(record.outflows.get(addr)) over that round's consumed set — outflows is a guaranteed field of the frozen 03-01 ThresholdRoundRecord contract; this plan does not modify demo/thresholdModel.ts); aggregate median + p10
    - excluded-paper latency: pool history.excludedLatencies across the cell's seeds; report mean and p95
    - abort_rate = aborted records / (settled + aborted) records across the cell
    - exclusion_round_fraction = settled-2pass records / all settled records across the cell
    - unsettled_fraction = sum unsettledCount / sum generatedCount across the cell
    CSV header exactly: n,p,density,reciprocity,seeds,rounds_per_seed,median_realized_compression,p10_realized_compression,median_worst_saving,p10_worst_saving,mean_excluded_latency_rounds,p95_excluded_latency_rounds,abort_rate,exclusion_round_fraction,unsettled_fraction — write to docs/sweep/threshold-sweep.csv (numbers to 4 decimals like v1). Log progress per (n, p) group with elapsed time. If the full run exceeds ~15 minutes locally, the first lever is the model's internal settled-id pruning of the open pool (permitted in 03-01, value-neutral); only then reduce ROUNDS_PER_SEED (never SEEDS, never the grid — D-03 is locked) and the CSV's rounds_per_seed column records what was actually used.
    Console output ends with the D-05 headline table: rows n in {15, 30, 50}, columns p in {1.0, 0.95, 0.9}, cells showing median realized compression and p10 worst saving at (density 0.5, reciprocity 0.8) — the p=1.0 column is the idealized contrast (and should closely track v1's grid2 numbers in docs/sweep/sweep.csv; print both for eyeballing). Do NOT massage any parameter to flatter the number — an ugly number is an equally valid deliverable (03-CONTEXT specifics).
    Add "sweep:threshold": "tsx demo/thresholdSweep.ts" to package.json scripts. Run the full sweep once and commit the generated docs/sweep/threshold-sweep.csv.
  </action>
  <verify>
    <automated>npm run sweep:threshold -- --quick && awk 'END { exit NR == 501 ? 0 : 1 }' docs/sweep/threshold-sweep.csv</automated>
  </verify>
  <acceptance_criteria>
    - docs/sweep/threshold-sweep.csv exists with exactly 1 header row + 500 data rows (5 n x 5 p x 20 density/reciprocity combos x 1 row) from a FULL 200-seed run (the --quick verify invocation above is followed by the full run before commit; final committed file has seeds column = 200 in every row)
    - Header row matches the 15 columns listed in the action verbatim
    - All five D-04 metric families present as columns: compression (median+p10), worst-participant saving (median+p10), excluded latency (mean+p95), abort_rate, exclusion_round_fraction
    - Console run prints the D-05 headline table for n=15/30/50 x p=1.0/0.95/0.9
    - package.json has the sweep:threshold script; npm test still green; npx tsc --noEmit clean
  </acceptance_criteria>
  <done>Full-grid CSV committed, headline table printing, CALB-01's empirical answer exists as data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Simulation + reporting only; no protocol surface changed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Repudiation/Info-quality | threshold-sweep.csv (submission numbers) | mitigate | Exact-match cross-validation against the real attemptRound (Task 1, D-02); no parameter massaging (explicit instruction); UNCALIBRATED labeling applied in 03-03 (D-07) |
| T-03-SC | Tampering | package installs | accept | D-09: zero new dependencies — no install tasks exist |
</threat_model>

<verification>
- `npx vitest run test/thresholdCrossValidation.test.ts` green with exact-equality assertions
- `npm run sweep:threshold` completes end-to-end; CSV has 501 lines with the exact header
- `npm test` and `npx tsc --noEmit` green
</verification>

<success_criteria>
- Faithfulness proof exists: real attemptRound and the model agree exactly on D-02's cell subset
- CALB-01 grid data committed at docs/sweep/threshold-sweep.csv (D-03 grid, D-04 metrics, 200 seeds/cell)
- D-05 headline table produced — realized compression under p=0.95/0.9 vs idealized p=1.0 at n=15/30/50
</success_criteria>

<output>
Create `.planning/phases/03-calibration-checkpoint-brief-checkpoint/03-02-SUMMARY.md` when done
</output>
