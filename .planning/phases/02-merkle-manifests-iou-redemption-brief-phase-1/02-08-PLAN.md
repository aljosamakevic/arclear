---
phase: 02-merkle-manifests-iou-redemption-brief-phase-1
plan: 08
type: execute
wave: 7
depends_on: [02-05, 02-07]
files_modified: [docs/PROTOCOL.md, docs/THREAT-MODEL.md, README.md]
autonomous: false
requirements: [MERK-01, MERK-02, MERK-03, MERK-04]
user_setup:
  - service: arc-testnet
    why: "End-of-phase V2 hub redeploys (D-11) need a funded deployer key on Arc Testnet"
    env_vars:
      - name: ARC_RPC_URL
        source: ".env (template in .env.example) — https://rpc.testnet.arc.network"
      - name: DEPLOYER_PK
        source: "Existing deployer key; fund with testnet USDC via faucet.circle.com (USDC is the gas token)"

must_haves:
  truths:
    - "docs/PROTOCOL.md specifies the merkle manifest construction as numbered rules (0x00/0x01 prefixes, ordered pairs, lone-node promotion, sentinel) and states explicitly it is NOT an RFC 6962 tree shape"
    - "docs/PROTOCOL.md specifies redemption: the on-chain staleness definition (absent from the last >= K executed rounds), the L-bounded coverage rule with its safety argument, contract-derived proof sets, post-expiry redemption semantics, and the honest limitation that redemption races the never-pausable withdraw (best-effort recovery of posted, still-present collateral)"
    - "The 'no individual IOU redemption' non-goal is superseded — no doc still claims redemption doesn't exist; keep-alive censorship griefing is documented as a known, out-of-scope-to-punish limitation"
    - "PROTOCOL.md states the showcase framing plainly: bilateral credit with a collateralized recovery path; K/RING/L appear labeled uncalibrated with the measured gas numbers reported"
    - "Fresh redemption-capable V2 hubs (USDC + EURC) are live on Arc Testnet with explicit gas discipline; v1 hubs AND Phase-1 V2 hub addresses remain listed (D-11); README hub table updated"
  artifacts:
    - path: "docs/PROTOCOL.md"
      provides: "Merkle manifest spec + redemption spec + griefing/limitation additions + non-goal supersession"
      contains: "collateralized recovery path"
    - path: "docs/THREAT-MODEL.md"
      provides: "New rows: second-preimage, double-claim both directions, keep-alive griefing, exit race, TOCTOU"
      contains: "redemption"
    - path: "README.md"
      provides: "Phase-2 V2 hub addresses (USDC + EURC) alongside v1 and Phase-1 rows"
      contains: "redeemIOU"
  key_links:
    - from: "docs/PROTOCOL.md redemption spec"
      to: "ClearingHubV2.redeemIOU gate order + tests in 02-05"
      via: "spec claims backed by shipped revert matrix and e2e"
      pattern: "K "
    - from: "README.md hub table"
      to: "redeployed V2 addresses on Arc Testnet"
      via: "arcscan-verifiable contract addresses"
      pattern: "0x"
---

<objective>
Document the manifest and redemption protocol as implementable spec (third parties must reproduce the construction and gate rules exactly), reconcile the threat model, redeploy the redemption-capable hubs to Arc Testnet for USDC and EURC (D-11), and close the phase with a human verification — the "Arclear Net v2" shippable moment (Phases 1+2 complete).

Purpose: MERK-01..04's documentation half and the phase's shipping act. The showcase story this phase must say plainly: redemption is what makes "a tab with a limit" honest — bilateral credit with a collateralized recovery path.
Output: updated docs/PROTOCOL.md, docs/THREAT-MODEL.md, README.md; two live redemption-capable V2 hubs on Arc Testnet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-CONTEXT.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-RESEARCH.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-PATTERNS.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-05-SUMMARY.md
@docs/V2-BRIEF.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PROTOCOL.md merkle + redemption spec; THREAT-MODEL.md reconciliation</name>
  <files>docs/PROTOCOL.md, docs/THREAT-MODEL.md</files>
  <read_first>
    - docs/PROTOCOL.md (full file — the "Manifest commitment" section being rewritten, the "Netting determinism spec" numbered-rule style to replicate, the "Threshold consent" griefing analysis to extend, the "Explicit non-goals" supersession pattern from Phase 1)
    - docs/THREAT-MODEL.md (full file — row format and limitations table; new rows must stay terminologically consistent)
    - .planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-RESEARCH.md — Pattern 2/3 (the construction/proof rules to specify), Critical Question 1 (the L-rule + safety argument + A5 assumption to document), Pattern 3 soundness note (A3 argument to write down for review), Pitfalls 2/3/4 (the three honesty items)
    - .planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-05-SUMMARY.md (measured gas table to report)
    - contracts/src/ClearingHubV2.sol redeemIOU NatSpec (the shipped gate order the spec must match exactly)
  </read_first>
  <action>
    docs/PROTOCOL.md: (1) Rewrite "Manifest commitment" as a numbered-rule spec: sorted unique lowercase-ascending IOU-id leaves; leaf = keccak256(0x00 ‖ id); node = keccak256(0x01 ‖ left ‖ right) ordered, never sorted; odd node promotes unchanged; empty manifest keeps the sentinel keccak256("0x"); state EXPLICITLY that only the RFC 6962 prefix scheme is borrowed and the tree shape (level-wise promotion) is NOT RFC 6962 — third parties implementing the RFC's split rule will diverge. Include the proof encodings (inclusion walk; below-first/above-last/bracket non-inclusion with strict inequalities) and the position-binding soundness argument (research A3: lying about index/leafCount changes the sibling-consumption schedule, so matching the root requires a keccak second preimage — written down for reviewability). (2) Add an "IOU redemption" section: purpose framing verbatim in spirit — bilateral credit with a collateralized recovery path (the phrase "collateralized recovery path" must appear); the gate as numbered rules matching the shipped order (trivia → staleness → coverage → signature → nullifier → proofs → effects) with exact parameter semantics: staleness = absent from the last >= K EXECUTED rounds (on-chain criterion; coordinator miss counters are an off-chain early-warning signal only — Pitfall 4); coverage = full-history OR executedAt(oldest buffered) < expiry − L, with the signing convention expiry <= signTime + L, its safety argument (consumption window [E−L, E) always buffered for honest debtors; a violating debtor weakens only their own protection), the A5 assumption (60s netting safety window >= proposal-to-execution latency), and fail-closed behavior (fast cadence narrows to unavailability — a liveness loss, never a safety loss; K/RING/L calibration explicitly deferred to Phase 3, labeled UNCALIBRATED); redemption valid before AND after expiry (expiry bounds netting, not recovery); exact-count contract-derived proof sets (TOCTOU: an intervening round reverts the redemption; regenerate); no partial redemption. (3) Honesty subsection: redemption recovers posted, still-present collateral only — it races the never-pausable withdraw and is best-effort by design (Pitfall 2; no language implying a guarantee); extend the griefing analysis with keep-alive censorship (a live debtor dust-cycling to keep lastRound fresh while censoring one creditor: visible on-chain, costs gas every round, out of protocol scope to punish in the Net product — Pitfall 3). (4) Report the measured gas table (executeRound m∈{10,105,250}, redeemIOU RING=16) from the 02-05 summary. (5) Supersede the "No individual IOU redemption" bullet in Explicit non-goals using the same supersession note pattern Phase 1 used for threshold consent. docs/THREAT-MODEL.md: add/update rows for merkle second-preimage (mitigated: prefixes + fixtures), double-claim net→redeem (L-coverage rule) and redeem→net (nullifier + executeRound check), forged position claims (schedule-binding + fuzz), keep-alive censorship griefing (documented limitation), debtor exit race (unfixable by design — withdraw never pausable), proof-set TOCTOU (count mismatch reverts); update any limitations-table entry describing provable claims or redemption as future work to shipped-in-v2 status, keeping terminology identical to PROTOCOL.md.
  </action>
  <verify>
    <automated>grep -q 'collateralized recovery path' docs/PROTOCOL.md && grep -qi 'not.*RFC 6962\|NOT an RFC 6962' docs/PROTOCOL.md && grep -qi 'UNCALIBRATED' docs/PROTOCOL.md && ! grep -qi 'no individual IOU redemption[^ ]* on-chain$' docs/PROTOCOL.md && grep -qi 'redemption' docs/THREAT-MODEL.md</automated>
  </verify>
  <acceptance_criteria>
    - PROTOCOL.md contains: the numbered merkle construction rules, the not-RFC-6962 warning, the full gate order matching the shipped contract, the L-convention + safety argument + A5 assumption, the on-chain K-window definition with counters demoted, the best-effort/withdraw-race honesty note, the keep-alive griefing addition, the measured gas table, and the phrase "collateralized recovery path"
    - The non-goals section supersedes individual-IOU-redemption with the Phase-1-style supersession note; no doc still claims redemption doesn't exist
    - THREAT-MODEL.md rows added/updated per the action, terminologically consistent with PROTOCOL.md (spot-check: "executed rounds" not "windows" for the on-chain gate)
    - No claim in either doc exceeds what plans 02-05/02-07 actually test (spec ↔ tests consistency)
  </acceptance_criteria>
  <done>Third parties can reimplement the manifest and redemption rules from PROTOCOL.md alone, and every honesty caveat the research mandated is in print.</done>
</task>

<task type="auto">
  <name>Task 2: Redeploy V2 hubs (USDC + EURC) to Arc Testnet and record addresses</name>
  <files>README.md</files>
  <read_first>
    - contracts/script/DeployV2.s.sol (updated usage doc-comment with HUB_K/HUB_RING/HUB_MAX_IOU_LIFETIME env vars from plan 02-04)
    - README.md (Quickstart + deployed-hubs table — v1 rows AND Phase-1 V2 rows to keep, per D-11)
    - .planning/phases/01-threshold-consent-brief-phase-0/01-05-SUMMARY.md (the Phase-1 deploy invocations, token addresses USDC 0x3600000000000000000000000000000000000000 / EURC 0x89B50855Aa3bE2F677cD6303Cec089B5F319D72a, Blockscout verify command + fallback)
  </read_first>
  <action>
    Run the full local gate first: `npm test && npm run test:contracts && npm run e2e:anvil` must be green before any testnet write. Then deploy with the uncalibrated defaults (K=3, RING=16, L=86400 — omit the env overrides so vm.envOr defaults apply): forge script contracts/script/DeployV2.s.sol --root contracts --rpc-url "$ARC_RPC_URL" --private-key "$DEPLOYER_PK" --broadcast --with-gas-price 25gwei, once with TOKEN_ADDRESS=0x3600000000000000000000000000000000000000 (USDC) and once with TOKEN_ADDRESS=0x89B50855Aa3bE2F677cD6303Cec089B5F319D72a (EURC). Explicit gas price is mandatory (Arc gas-token gotcha). If .env/ARC_RPC_URL/DEPLOYER_PK are missing or unfunded, STOP and create an auth-gate checkpoint asking the user to populate .env (faucet.circle.com) — never skip the deploy silently. After each deploy: `cast code <address> --rpc-url "$ARC_RPC_URL"` must return non-empty bytecode; attempt Blockscout verification (`forge verify-contract --verifier blockscout --verifier-url https://testnet.arcscan.app/api <address> ClearingHubV2` with the constructor args encoded) and note failures for the checkpoint rather than blocking. Update README.md: add the two new redemption-capable hub addresses to the deployed-hubs table labeled as the active Arclear Net v2 hubs (note K/RING/L values and their uncalibrated status), KEEP the v1 rows and the Phase-1 V2 rows listed (D-11 — mark the Phase-1 V2 hubs as superseded-but-live), and add a short "IOU redemption" bullet to the feature summary referencing PROTOCOL.md (must mention redeemIOU). Do NOT write addresses into .env.
  </action>
  <verify>
    <automated>cast code $(grep -oE '0x[a-fA-F0-9]{40}' README.md | tail -2 | head -1) --rpc-url "$ARC_RPC_URL" | grep -qv '^0x$'</automated>
  </verify>
  <acceptance_criteria>
    - Full local gate (npm test, test:contracts, e2e:anvil) green before broadcast
    - Two broadcasts succeeded with --with-gas-price 25gwei; `cast code` non-empty for both new addresses
    - README table lists new Phase-2 hubs AND retains v1 + Phase-1 V2 rows; K/RING/L noted as uncalibrated
    - No transaction sent to any v1 or Phase-1 hub address
  </acceptance_criteria>
  <done>Arclear Net v2 with provable manifests and IOU redemption is live on Arc Testnet for USDC and EURC; the lineage of hubs is honestly recorded (D-11).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the live deployment and the redemption story</name>
  <files>none (verification only)</files>
  <action>Pause for human verification of the live Arc Testnet redeploy and the redemption walkthrough described in how-to-verify. No file changes; all automation completed in Tasks 1-2.</action>
  <verify>
    <human-check>User completes the walkthrough in how-to-verify and responds "approved" or reports issues</human-check>
  </verify>
  <done>User has typed "approved" after confirming arcscan contract pages, the e2e redemption scenario, and the PROTOCOL.md manifest/redemption spec.</done>
  <what-built>ClearingHubV2 extended with sorted-leaf merkle manifests (TS↔Solidity byte-parity-proven), on-chain participation tracking, a ring buffer of round roots, and redeemIOU — deployed fresh to Arc Testnet for USDC and EURC with uncalibrated K=3/RING=16/L=24h. The e2e proves: dark debtor → creditor redeems from calldata-reconstructed proofs → redeemed IOU can never net again. Phases 1+2 together are the shippable "Arclear Net v2".</what-built>
  <how-to-verify>
    1. Open https://testnet.arcscan.app and search both new V2 addresses from the README table — confirm contract code exists (and source verification status; if Blockscout verification failed in Task 2, verify manually via the explorer UI).
    2. Run `npm run e2e:anvil` locally — confirm the redemption scenario lines pass: staleness precondition, exact debit/credit, redeemed(id) true, id absent from later manifests.
    3. Run `npm run demo -- --anvil`, open http://localhost:4402, click Simulate then Run Round — confirm rounds still settle normally end-to-end (no regression from the executeRound ABI change).
    4. Skim docs/PROTOCOL.md's new "Manifest commitment" and "IOU redemption" sections — confirm the construction is specified as numbered rules with the not-RFC-6962 warning, the gate order matches what you watched the e2e do, and the honesty notes (withdraw race, keep-alive griefing, uncalibrated K/RING/L) read plainly.
    5. Check README's hub table: new hubs listed with uncalibrated params, v1 and Phase-1 rows still present.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe issues (e.g. unverified contracts, spec drift, e2e failures)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy machine → Arc Testnet | Broadcast with DEPLOYER_PK; USDC is both gas token and ERC-20 |
| docs → third-party implementers | Spec drift from shipped behavior breaks reimplementations and audits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-32 | DoS | unbounded gas estimation on deploy (USDC gas token) | mitigate | --with-gas-price 25gwei mandated in the Task 2 invocation |
| T-02-33 | Info Disclosure | DEPLOYER_PK leakage | mitigate | Key read from gitignored .env only; never echoed into README/logs |
| T-02-34 | Repudiation | docs overselling redemption as guaranteed recovery | mitigate | Task 1 mandatory best-effort/withdraw-race language (Pitfall 2); acceptance criterion forbids guarantee language |
| T-02-35 | Tampering | deploying drifted bytecode | mitigate | Full local gate (tests + parity + e2e) required green before broadcast |
| T-02-36 | Repudiation | spec claims exceeding tested behavior | mitigate | Task 1 acceptance: every spec claim backed by 02-05/02-07 tests; human checkpoint cross-reads spec vs e2e |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `grep 'collateralized recovery path' docs/PROTOCOL.md` matches
- `npm test && npm run test:contracts && npm run e2e:anvil` green before deploy
- Both new addresses return non-empty `cast code` on Arc Testnet
- Human checkpoint approved (arcscan + e2e + spec walkthrough)
</verification>

<success_criteria>
- MERK-01..04 documented as implementable spec, consistent with THREAT-MODEL.md and the shipped gate order
- D-11 complete: fresh USDC + EURC hubs live with uncalibrated params recorded; v1 and Phase-1 hubs untouched and still listed
- Phase 2 = shippable Arclear Net v2: provable claims + collateralized recovery path, honestly framed
</success_criteria>

<output>
Create `.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-08-SUMMARY.md` when done
</output>
