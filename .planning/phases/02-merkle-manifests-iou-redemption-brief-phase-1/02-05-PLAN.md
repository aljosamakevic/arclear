---
phase: 02-merkle-manifests-iou-redemption-brief-phase-1
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified: [contracts/test/utils/RoundBuilderV2.sol, contracts/test/ClearingHubV2.t.sol]
autonomous: true
requirements: [MERK-03, MERK-04]
must_haves:
  truths:
    - "A V2 test harness exists (RoundBuilder pins v1 — new file, not an edit) building rounds with consumedIds and signing IOUs with a local EIP-712 mirror"
    - "redeemIOU's full revert matrix passes: staleness, coverage fail-closed, bad signature, nullifier, proof count/positional mismatch, insufficient collateral, trivia guards, pause boundary"
    - "Exclusivity is proven both directions on-chain: a redeemed id in a manifest reverts executeRound; an id included in a buffered round cannot produce a valid non-inclusion proof set"
    - "Gas is measured (not estimated) for executeRound at m∈{10,105,250} and redeemIOU at RING=16, with numbers recorded in the SUMMARY for plan 02-06's client gas formula"
  artifacts:
    - path: "contracts/test/utils/RoundBuilderV2.sol"
      provides: "V2 harness: ClearingHubV2 deploy with (K,RING,L), consumedIds round helpers, _signIou + _iouDigest mirror"
      contains: "ClearingHubV2"
    - path: "contracts/test/ClearingHubV2.t.sol"
      provides: "Revert matrix, exclusivity both directions, fuzz, gas tests"
      contains: "testFuzz_"
  key_links:
    - from: "contracts/test/ClearingHubV2.t.sol"
      to: "contracts/test/utils/RoundBuilderV2.sol"
      via: "harness inheritance"
      pattern: "is RoundBuilderV2"
    - from: "gas tests"
      to: "plan 02-06 client gas formula"
      via: "measured numbers recorded in 02-05-SUMMARY.md"
      pattern: "test_gas"
---

<objective>
Prove the plan-02-04 contract behavior with a dedicated V2 harness, the full redeemIOU revert matrix, both exclusivity directions (MERK-04), adversarial fuzz, and measured gas numbers (CONTEXT requires the executeRound gas delta measured and reported; STATE.md flags the eroding 1.5M headroom).

Purpose: MERK-03/MERK-04's test obligations, plus the empirical gas coefficients plan 02-06 needs for the size-parameterized client limit.
Output: contracts/test/utils/RoundBuilderV2.sol, contracts/test/ClearingHubV2.t.sol, gas numbers in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-CONTEXT.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-RESEARCH.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-PATTERNS.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RoundBuilderV2.sol harness</name>
  <files>contracts/test/utils/RoundBuilderV2.sol</files>
  <read_first>
    - contracts/test/utils/RoundBuilder.sol (full file — MockUSDC to IMPORT not duplicate, actor derivation + address sort :29-52, _signRound :69-79, the local _digest EIP-712 mirror :82-111 whose recipe the IOU mirror follows)
    - contracts/src/ClearingHubV2.sol (new constructor, Iou struct, executeRound consumedIds signature, redeemIOU + errors — from plan 02-04)
    - .planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-PATTERNS.md — "contracts/test/utils/RoundBuilderV2.sol" section
  </read_first>
  <action>
    Create contracts/test/utils/RoundBuilderV2.sol as a NEW file (v1 tests keep the original — PATTERNS caveat 1). Import MockUSDC from RoundBuilder.sol. Copy the actor derivation/funding/deposit setup, deploying `hub = new ClearingHubV2(usdc, 3, 16, 86400)` (defaults matching DeployV2). Helpers: _simpleRound returning (participants, deltas, consumedIds) where consumedIds is a small strictly-ascending bytes32 set; _buildSignatures over the digest computed with the derived root (call hub.hashRound with ManifestMerkle.rootOf(consumedIds) or mirror locally like _digest); _executeRoundWithout(address absent) — executes a zero-sum round among actors EXCLUDING `absent` with empty or non-overlapping consumedIds, used to advance roundNonce and stale-out a debtor on the ON-CHAIN clock (Pitfall 4: eligibility is executed-rounds-without-participation, never coordinator counters); _signIou(uint256 pk, ClearingHubV2.Iou memory iou) mirroring _signRound via a local _iouDigest that reproduces the EIP-712 domain ("ArcClearingHub", "1", block.chainid, address(hub)) with the IOU typehash; _makeIou(debtor, creditor, amount, nonce, expiry) convenience honoring the L-convention (expiry <= block.timestamp + 86400) by default; _proofsFor(bytes32 id) building the full ascending non-inclusion proof array over the currently buffered roots by replaying stored consumedIds per round (track executed rounds' id sets in harness storage as they execute). Internal helpers _-prefixed per convention.
  </action>
  <verify>
    <automated>cd contracts && forge build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - RoundBuilderV2.sol compiles; MockUSDC imported from RoundBuilder.sol (grep shows no duplicate MockUSDC contract definition)
    - Harness exposes _simpleRound, _buildSignatures, _executeRoundWithout, _signIou, _iouDigest, _makeIou, _proofsFor
    - _iouDigest reproduces the domain exactly as RoundBuilder._digest does for rounds (same name/version/chainid/hub recipe)
  </acceptance_criteria>
  <done>All V2 test files can build/sign rounds with consumedIds, sign IOUs, advance on-chain staleness, and construct complete proof sets natively.</done>
</task>

<task type="auto">
  <name>Task 2: redeemIOU revert matrix + exclusivity both directions</name>
  <files>contracts/test/ClearingHubV2.t.sol</files>
  <read_first>
    - contracts/test/ClearingHub.t.sol (revert-matrix idiom :105-118, invariant assertions after settlement :75-81 incl. hub-token-balance conservation, event assertion :71-73, test_withdraw_worksWhilePaused :41-47)
    - contracts/src/ClearingHubV2.sol redeemIOU gate order and exact error signatures (plan 02-04)
    - .planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-RESEARCH.md — Validation Architecture MERK-03/MERK-04 rows, Pitfall 6 (never-participated boundaries)
  </read_first>
  <action>
    Create contracts/test/ClearingHubV2.t.sol inheriting RoundBuilderV2, section-divider comments per convention. executeRound evolution tests: test_executeRound_writesRootRing (assert StoredRoot root/nonce/executedAt fields), test_executeRound_writesLastRoundForAllParticipants (including a zero-delta participant — assert lastRound == nonce+1 for each), test_revert_executeRound_unsortedConsumedIds (UnsortedLeaves), test_revert_executeRound_nullifiedId (redeem an IOU first via the full happy path, then a round consuming that id reverts NullifiedIdInManifest — the redeem→cannot-net direction, D-14). redeemIOU happy path: test_redeemIOU_debitsStaleDebtor — stale the debtor via K=3 _executeRoundWithout calls, redeem with _proofsFor, assert debtor debited exactly amount, creditor credited, hub USDC balance conserved, redeemed(id) true, IouRedeemed event via vm.expectEmit. Revert matrix (test_revert_redeemIOU_*): notStale (debtor participated recently → DebtorNotStale with diagnostic values), neverParticipatedBoundary (lastRound==0: stale iff roundNonce+1 >= K — test BOTH sides of the boundary, Pitfall 6), coverageNotBuffered (execute RING+1 = 17 rounds so eviction occurred, use an iou whose expiry makes executedAt(oldest) >= expiry - L → CoverageWindowNotBuffered; plus the expiry <= L fail-closed underflow branch), badSignature (sig from non-debtor key → BadIouSignature), alreadyRedeemed (second redemption → AlreadyRedeemed), proofCountMismatch (drop one proof → ProofCountMismatch(expected, provided)), nonInclusionInvalid (an IOU actually consumed in a buffered round: its _proofsFor cannot produce a valid proof for the containing round → NonInclusionProofInvalid — the structural net→cannot-redeem direction, MERK-04/D-15), insufficientCollateral (debtor withdrew first → InsufficientCollateral; doubles as the honest withdraw-race documentation test), zeroAmount, selfIou. Pause boundary: test_redeemIOU_revertsWhilePaused + test_withdraw_worksWhilePaused_V2 (withdraw NEVER pausable, D-12).
  </action>
  <verify>
    <automated>cd contracts && forge test --match-contract ClearingHubV2 -vvv 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Every test named in the action exists and passes; parameterized reverts asserted via abi.encodeWithSelector with exact diagnostic values
    - Both exclusivity directions proven: test_revert_executeRound_nullifiedId AND test_revert_redeemIOU_nonInclusionInvalid
    - Conservation asserted in the happy path (hub token balance unchanged by redemption)
    - Never-participated boundary tested on both sides (redeems at roundNonce+1 == K, reverts at roundNonce+1 == K-1)
  </acceptance_criteria>
  <done>MERK-03's revert matrix and MERK-04's bidirectional exclusivity are locked by forge tests against the real gate order and error signatures.</done>
</task>

<task type="auto">
  <name>Task 3: Fuzz + gas measurement</name>
  <files>contracts/test/ClearingHubV2.t.sol</files>
  <read_first>
    - contracts/test/ClearingHubFuzz.t.sol :68-92 (testFuzz_perturbationAlwaysReverts — seed-driven tamper + state-untouched assertion pattern)
    - .planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-RESEARCH.md — Critical Question 4 (gas table, the m∈{10,105,250} + RING=16 measurement mandate, the BASE + PER_PARTICIPANT·n + PER_ID·m formula plan 02-06 will implement)
  </read_first>
  <action>
    Extend contracts/test/ClearingHubV2.t.sol. Fuzz (512 runs): testFuzz_redeemProofSetSkip_reverts (fuzz-chosen proof index removed or swapped → always reverts, hub state untouched after — assert roundNonce and both collateral balances unchanged, ClearingHubFuzz pattern), testFuzz_redeemNullifierIdempotent (after a successful redemption, fuzz-perturbed re-attempts always revert AlreadyRedeemed and balances never move again), testFuzz_redeemProofPerturbation_reverts (perturb index/leafCount/one sibling of a fuzz-chosen proof in an otherwise-valid set → revert). Gas: test_gas_executeRound_m10, test_gas_executeRound_m105, test_gas_executeRound_m250 (n=5 participants each, consumedIds sized 10/105/250, measure via vm.snapshotGasLastCall or gasleft() deltas around the call and emit console2.log with a stable label), test_gas_redeemIOU_ring16 (execute 16 rounds so the full ring is populated, then measure a redemption carrying 16 proofs). Run `forge snapshot --match-test test_gas` to persist .gas-snapshot entries. RECORD in the 02-05 SUMMARY: the four measured numbers, the per-id and per-participant deltas derived from them, and the recommended client-side coefficients with >=1.5x margin (BASE/PER_PARTICIPANT/PER_ID for executeRound, flat limit for redeemIOU) — plan 02-06 consumes these verbatim. Flag loudly in the SUMMARY if m=105 executeRound exceeds ~1.1M gas (the v1 1.5M headroom concern from STATE.md).
  </action>
  <verify>
    <automated>cd contracts && forge test --match-contract ClearingHubV2 2>&1 | tail -4 && forge snapshot --match-test test_gas 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All three fuzz tests pass at 512 runs with state-untouched assertions after expected reverts
    - Four test_gas_* tests exist; `forge snapshot --match-test test_gas` writes entries
    - Full `cd contracts && forge test` suite green
    - SUMMARY contains the measured gas table + derived client coefficients (this is CONTEXT's "measured and reported" requirement)
  </acceptance_criteria>
  <done>Adversarial robustness is fuzz-proven and the gas envelope is measured, not assumed — coefficients ready for the 02-06 client formula.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fuzz harness → contract | Simulates adversarial provers/relayers against the redemption gate |
| gas assumptions → Arc reality | Estimated EVM costs (research A1/A2) must be replaced by measurement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-21 | Elevation | proof-set manipulation slipping past positional matching | mitigate | testFuzz_redeemProofSetSkip_reverts + perturbation fuzz (research A3 mandate) |
| T-02-22 | Tampering | nullifier bypass via repeated/perturbed redemption | mitigate | testFuzz_redeemNullifierIdempotent with balance-freeze assertions |
| T-02-23 | DoS | executeRound gas breaching the 1.5M client limit at demo scale | mitigate | Measured m∈{10,105,250} + explicit headroom flag in SUMMARY (Q4) |
| T-02-24 | Repudiation | claiming exclusivity without on-chain proof | mitigate | Both directions tested against real state (Task 2), referenced by PROTOCOL.md in plan 02-08 |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `cd contracts && forge test` fully green at 512 fuzz runs
- `forge snapshot --match-test test_gas` persisted
- SUMMARY carries measured numbers + client coefficients
</verification>

<success_criteria>
- MERK-03: full revert matrix green against exact error signatures
- MERK-04: nullifier idempotence + bidirectional exclusivity proven on-chain
- Gas measured at the mandated sizes; coefficients handed to plan 02-06
</success_criteria>

<output>
Create `.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-05-SUMMARY.md` when done
</output>
