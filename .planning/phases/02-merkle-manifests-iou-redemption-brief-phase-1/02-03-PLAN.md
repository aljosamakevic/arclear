---
phase: 02-merkle-manifests-iou-redemption-brief-phase-1
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified: [src/round.ts, test/genFixture.ts, test/fixtures/digest.json, test/fixtures/merkle.json, contracts/test/MerkleParity.t.sol]
autonomous: true
requirements: [MERK-01, MERK-02]
must_haves:
  truths:
    - "manifestHash() in src/round.ts now returns the sorted-leaf merkle root — same exported name/signature, same bytes32 field, Round digest struct byte-identical (MERK-01)"
    - "test/fixtures/merkle.json carries shared root + inclusion + non-inclusion vectors that both vitest and Foundry consume, proving TS↔Solidity byte-parity (MERK-02)"
    - "digest.json regenerated via npm run fixture (values change, encoding unchanged) and all existing digest-parity tests stay green"
    - "digest.json now carries the full fixture Iou fields and its debtor signature (iouSig) for the on-chain hashIou parity landing in plan 02-04"
  artifacts:
    - path: "src/round.ts"
      provides: "manifestHash delegating to merkleRoot"
      contains: "merkle.js"
    - path: "test/fixtures/merkle.json"
      provides: "Cross-stack merkle vectors for leaf counts {0,1,2,3,5,8} incl. negative + uppercase vectors"
    - path: "contracts/test/MerkleParity.t.sol"
      provides: "Foundry parity test reading merkle.json against ManifestMerkle"
      contains: "vm.readFile"
    - path: "test/fixtures/digest.json"
      provides: "Regenerated digest fixture + iou fields + iouSig"
      contains: "iouSig"
  key_links:
    - from: "src/round.ts"
      to: "src/merkle.ts"
      via: "import { merkleRoot } from ./merkle.js"
      pattern: "merkleRoot"
    - from: "contracts/test/MerkleParity.t.sol"
      to: "test/fixtures/merkle.json"
      via: "vm.readFile + fs_permissions (already granted)"
      pattern: "merkle\\.json"
    - from: "test/genFixture.ts"
      to: "test/fixtures/merkle.json + digest.json"
      via: "npm run fixture regeneration (never hand-edited)"
      pattern: "merkle"
---

<objective>
Swap the manifest preimage to the merkle root at its single choke point (MERK-01), extend the fixture generator to emit shared merkle vectors + the fixture IOU's signature, and lock TS↔Solidity byte-parity with a Foundry parity test (MERK-02, D-16).

Purpose: this is the moment the two independent implementations from wave 1 are proven byte-identical, and the moment `manifestHash` becomes provable. Sequencing is exact (PATTERNS caveat 4): swap → `npm run fixture` regenerate → parity green. digest.json VALUES change; the encoding D-11 protects does not.
Output: modified src/round.ts + test/genFixture.ts; regenerated digest.json; new merkle.json + MerkleParity.t.sol.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-CONTEXT.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-RESEARCH.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap manifestHash body to merkleRoot</name>
  <files>src/round.ts</files>
  <read_first>
    - src/round.ts lines 1-70 and 170-190 (manifestHash at :19-22 with its sentinel branch and the doc comment anticipating this swap; buildProposal call at :58; verifyProposal root check at :179-181)
    - src/merkle.ts (wave-1 module — merkleRoot already handles the empty-list sentinel internally, D-04)
    - .planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-RESEARCH.md — Pattern 1 "Single choke-point root swap" and Pitfall 1 (fixture regeneration is expected, not breakage)
  </read_first>
  <action>
    In src/round.ts, import merkleRoot from "./merkle.js" and replace the body of manifestHash(sortedIds) with a single delegation to merkleRoot(sortedIds) — merkleRoot's internal sentinel branch preserves keccak256("0x") for empty input, so the explicit branch can be removed. Keep the exported name `manifestHash` and signature `(sortedIds: Hex[]): Hex` unchanged so buildProposal (:58), verifyProposal (:179), and test/genFixture.ts (:40) are untouched. Rewrite the doc comment at :14-18: the preimage is now the sorted-leaf merkle root (construction spec: src/merkle.ts numbered rules / docs/PROTOCOL.md); same bytes32 field, Round EIP-712 struct unchanged. Do NOT touch ROUND_TYPES, roundDigest, buildProposal, rebuildProposal, or verifyProposal logic — the verifyProposal recomputation transparently becomes a root check (this preserves the Phase-1 hardened nonce/overlap pinning; verify no diff outside manifestHash + its doc comment + the import line).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - git diff for src/round.ts shows changes ONLY in: import block, manifestHash body, manifestHash doc comment
    - manifestHash([]) still equals keccak256("0x") (existing tests + fixture regeneration confirm)
    - npm test green — rebuild.test.ts and eip712.test.ts pass unmodified (they recompute both sides)
  </acceptance_criteria>
  <done>MERK-01's SDK half: the manifest preimage is a merkle root in the same bytes32 field, with zero digest-struct change and zero call-site churn.</done>
</task>

<task type="auto">
  <name>Task 2: Extend genFixture — merkle.json vectors + iou/iouSig in digest.json — and regenerate</name>
  <files>test/genFixture.ts, test/fixtures/digest.json, test/fixtures/merkle.json</files>
  <read_first>
    - test/genFixture.ts (full file, 72 lines — deterministic accounts, the canonical Iou at :30-37, serialization/write pattern at :55-72)
    - .planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-RESEARCH.md — Pattern 5 "Fixture-parity extension" (vector set specification) and Q5a (iouSig obligation)
    - .planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-PATTERNS.md — "contracts/test/MerkleParity.t.sol" section (flat JSON paths for vm.parseJson addressing — shape merkle.json accordingly)
    - src/merkle.ts (proof generation functions and types)
    - src/iou.ts (signIou signature — debtor must be the signer)
  </read_first>
  <action>
    Extend test/genFixture.ts (extend, never fork): (1) add to the digest.json fixture object: the full `iou` fields as a flat group (iouDebtor, iouCreditor, iouAmount stringified, iouNonce stringified, iouExpiry stringified, iouRef) and `iouSig` produced by `signIou(HUB, iou, accounts[0])` — accounts[0] IS the debtor (participants[0]); keep every existing field. (2) emit test/fixtures/merkle.json with FLAT vm.parseJson-addressable keys: for each leaf count n in {0,1,2,3,5,8} under key prefix `caseN`: `caseN_ids` (sorted lowercase bytes32 array — derive deterministically from keccak of fixed strings), `caseN_root`; for n=8 additionally: one inclusion proof per leaf as `case8_inc{i}_leaf/index/leafCount/siblings`, and non-inclusion vectors `case8_niBelow_*`, `case8_niAbove_*`, `case8_niBracket_*` (each carrying kind as uint 0/1/2, targetId, and the a/b proof fields flattened); a NEGATIVE vector `case8_negMemberId` = an id that IS a leaf (parity test must assert verifyNonInclusion false); and an UPPERCASE vector: `caseUpper_inputIds` (one id with uppercase hex) + `caseUpper_root` proving the TS normalization produced the same root as lowercase (Pitfall 7 lock). Then run `npm run fixture` — digest.json manifestHash/digest/consent0 values WILL change (merkle root swap); this is the expected pipeline, never hand-edit any fixture value. Run the Foundry digest parity suite to confirm the encoding survived.
  </action>
  <verify>
    <automated>npm run fixture && npm test 2>&1 | tail -3 && cd contracts && forge test --match-contract 'DigestParity|ClearingHubV2Parity' 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - test/fixtures/digest.json contains iouSig and the six flat iou field keys, plus all pre-existing keys
    - test/fixtures/merkle.json exists with all case0..case8, negative, and uppercase vector keys named in the action
    - digest.json manifestHash differs from its pre-swap value (git diff shows regeneration), and `forge test --match-contract 'DigestParity|ClearingHubV2Parity'` is green against the regenerated values
    - No fixture value hand-edited: re-running `npm run fixture` twice produces byte-identical output (deterministic)
  </acceptance_criteria>
  <done>Shared vectors exist for both stacks; the regenerated digest fixture proves MERK-01's encoding invariant; iouSig is staged for plan 02-04's on-chain hashIou parity.</done>
</task>

<task type="auto">
  <name>Task 3: MerkleParity.t.sol — Foundry side of the byte-parity lock</name>
  <files>contracts/test/MerkleParity.t.sol</files>
  <read_first>
    - contracts/test/DigestParity.t.sol (full file, 40 lines — the vm.readFile/vm.parseJson* fixture-read pattern to replicate; fs_permissions already grant ../test/fixtures)
    - test/fixtures/merkle.json (generated in Task 2 — the exact key names to parse)
    - contracts/src/lib/ManifestMerkle.sol (rootOf/verifyInclusion/verifyNonInclusion surface and struct layouts)
  </read_first>
  <action>
    Create contracts/test/MerkleParity.t.sol: a bare Test contract (no hub deployment needed — pure library parity). test_rootParity: for each caseN, parse `caseN_ids` via vm.parseJsonBytes32Array and assert `ManifestMerkle.rootOf(ids) == caseN_root` ("TS and Solidity merkle roots diverge" message); case0 must equal ManifestMerkle.EMPTY_ROOT. test_inclusionParity: reconstruct each case8 InclusionProof from the flat keys and assert verifyInclusion true against case8_root. test_nonInclusionParity: reconstruct the below/above/bracket NonInclusionProof vectors (kind uint → enum) and assert verifyNonInclusion true for each targetId. test_negativeVector: assert verifyNonInclusion(case8_negMemberId, <bracket-style attempt around it built from the fixture proofs>, case8_root) returns false — a member id must never prove non-inclusion. test_uppercaseNormalizationParity: assert rootOf over the (lowercased-at-generation) caseUpper ids equals caseUpper_root, documenting that Solidity bytes32 numeric order == the SDK's lowercase-hex sort order. Follow DigestParity's assertEq-with-message style and test_ naming.
  </action>
  <verify>
    <automated>cd contracts && forge test --match-contract MerkleParity -vvv 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - All five test functions pass reading ONLY test/fixtures/merkle.json (no hardcoded roots)
    - No foundry.toml change needed (existing fs_permissions used)
    - `cd contracts && forge test` full suite green including ManifestMerkle, DigestParity, ClearingHubV2Parity
  </acceptance_criteria>
  <done>MERK-02 fully proven: byte-identical roots and proof verification across TS and Solidity, locked by regenerable shared fixtures (D-16).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture generator → both test stacks | Hand-edited fixtures could mask real divergence between implementations |
| TS root ↔ Solidity root | Any construction drift here silently breaks non-inclusion soundness later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | hand-edited fixture values masking parity breakage | mitigate | Regeneration-only rule (Pitfall 1); determinism acceptance check (double-run byte-identical) |
| T-02-10 | Tampering | TS/Solidity construction drift (case order, promotion, prefixes) | mitigate | merkle.json vectors incl. uppercase + negative vectors; bracket vector is the commutativity regression tripwire |
| T-02-11 | Repudiation | Round digest encoding silently changed by the swap | mitigate | DigestParity + ClearingHubV2Parity green against regenerated values (MERK-01 proof, D-11) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `npm run fixture` deterministic (double-run identical)
- `npm test` green; `cd contracts && forge test` full suite green
- git diff of src/round.ts confined to import + manifestHash body/doc
</verification>

<success_criteria>
- MERK-01: merkle root fills the same bytes32 field; Round struct/digest encoding proven unchanged by regenerated parity fixtures
- MERK-02: TS↔Solidity byte-parity locked for roots, inclusion, and non-inclusion incl. negative and uppercase vectors
- iouSig + iou fields staged in digest.json for plan 02-04's hashIou parity
</success_criteria>

<output>
Create `.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-03-SUMMARY.md` when done
</output>
