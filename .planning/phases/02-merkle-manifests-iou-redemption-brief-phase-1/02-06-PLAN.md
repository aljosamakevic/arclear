---
phase: 02-merkle-manifests-iou-redemption-brief-phase-1
plan: 06
type: execute
wave: 5
depends_on: [02-04, 02-05]
files_modified: [src/abi/ClearingHubV2.ts, src/client.ts, src/iou.ts, src/netting.ts, src/domain.ts, demo/setup.ts, test/genFixture.ts, test/fixtures/digest.json, test/eip712.test.ts, test/rebuild.test.ts]
autonomous: true
requirements: [MERK-03, MERK-04]
must_haves:
  truths:
    - "HubClient submits executeRound with consumedIds against the regenerated V2 ABI, using a size-parameterized explicit gas formula built from plan 02-05's measured coefficients (never estimation — Arc gas-token gotcha)"
    - "HubClient exposes redeemIOU (explicit gas), the new reads (lastRound, redeemed, rootRing, hashIou), fetchManifest (calldata reconstruction — zero coordinator trust), and prepareRedemptionProofs (contract-derived buffered range)"
    - "signIou enforces the L-convention expiry <= signTime + L (throws), with a { ok, reason } checkIouLifetime helper for verification paths (D-15's SDK half)"
    - "net() opts accept redeemedIds, excluded from netting exactly like settledIds (D-14's SDK half)"
  artifacts:
    - path: "src/abi/ClearingHubV2.ts"
      provides: "Regenerated ABI + bytecode for the extended contract"
      contains: "redeemIOU"
    - path: "src/client.ts"
      provides: "V2-bound HubClient: consumedIds executeRound, redeemIOU, reads, fetchManifest, prepareRedemptionProofs, gas formula"
      contains: "clearingHubV2Abi"
    - path: "src/iou.ts"
      provides: "checkIouLifetime + signIou L-enforcement"
      contains: "checkIouLifetime"
    - path: "src/netting.ts"
      provides: "redeemedIds opt in net()"
      contains: "redeemedIds"
  key_links:
    - from: "src/client.ts"
      to: "src/abi/ClearingHubV2.ts"
      via: "clearingHubV2Abi import replacing v1 binding for hub methods"
      pattern: "clearingHubV2Abi"
    - from: "src/client.ts fetchManifest"
      to: "executeRound calldata"
      via: "viem decodeFunctionData"
      pattern: "decodeFunctionData"
    - from: "src/client.ts prepareRedemptionProofs"
      to: "src/merkle.ts nonInclusionProof"
      via: "per-buffered-round proof construction"
      pattern: "nonInclusionProof"
---

<objective>
Wire the SDK to the extended hub: regenerate the V2 ABI, rebind HubClient to it (it currently binds the v1 ABI — PATTERNS caveat 2), add the redemption read/write/orchestration surface with measured explicit gas, and land the two SDK-side protocol halves — the L signing convention (D-15) and redeemed-id filtering in net() (D-14).

Purpose: MERK-03's creditor tooling and MERK-04's off-chain filtering half. After this plan the demo layer (02-07) only orchestrates.
Output: regenerated src/abi/ClearingHubV2.ts; extended client.ts, iou.ts, netting.ts, domain.ts; call-site audits keeping tsc/vitest green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-CONTEXT.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-RESEARCH.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-PATTERNS.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate V2 ABI and rebind HubClient writes with the measured gas formula</name>
  <files>src/abi/ClearingHubV2.ts, src/client.ts, demo/setup.ts</files>
  <read_first>
    - src/abi/ClearingHubV2.ts (current file format — abi const + bytecode export shape to reproduce from contracts/out after forge build)
    - src/client.ts (full file — v1 abi binding at :12, read pattern :32-45 incl. the uint64 BigInt coercion on roundNonce, write pattern :97-118 with maxFeePerGas + hardcoded gas: 1_500_000n)
    - .planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-05-SUMMARY.md (the measured gas coefficients — consume verbatim)
    - .planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-PATTERNS.md — "src/client.ts" section (ABI-binding caveat, formula mandate)
    - src/domain.ts :28 (MIN_MAX_FEE_PER_GAS)
  </read_first>
  <action>
    Run `cd contracts && forge build`, then regenerate src/abi/ClearingHubV2.ts from contracts/out/ClearingHubV2.sol/ClearingHubV2.json preserving the existing file's export shape (clearingHubV2Abi const assertion + clearingHubV2Bytecode). In src/client.ts: rebind ALL HubClient methods to clearingHubV2Abi (HubClient's consumers — demo, e2e — target V2; keep the existing `export { clearingHubAbi }` re-export untouched for v1 integrators). executeRound: args become [proposal.roundNonce, proposal.participants, proposal.deltas, proposal.consumedIds, signatures] (RoundProposal already carries consumedIds — verified src/types.ts:46); replace `gas: 1_500_000n` with `gas: EXECUTE_ROUND_GAS_BASE + EXECUTE_ROUND_GAS_PER_PARTICIPANT * BigInt(n) + EXECUTE_ROUND_GAS_PER_ID * BigInt(m)` where the three exported bigint constants come from the 02-05 measured coefficients (>=1.5x margin, doc comment citing the measured forge-snapshot values and date). Add redeemIOU(wallet, iou: Iou, sig: Hex, proofs: NonInclusionProof[]): Promise<Hex> — writeContract with explicit `gas: REDEEM_IOU_GAS` (exported constant from the measured RING=16 number with >=2x margin) + maxFeePerGas: MIN_MAX_FEE_PER_GAS; convert TS proof types to ABI tuples (index/leafCount → bigint, kind → 0|1|2) in a module-private mapper. Method-level signature of executeRound(wallet, proposal, signatures) unchanged so coordinator call sites survive untouched. One demo-side compile fix belongs here (tsconfig type-checks demo/): demo/setup.ts's anvil deployContract currently passes args: [token] — extend to [token, 3n, 16n, 86_400n] (the same uncalibrated defaults as DeployV2.s.sol) so tsc stays green; all other demo wiring waits for plan 02-07.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/abi/ClearingHubV2.ts contains executeRound with consumedIds input, redeemIOU, hashIou, rootRing, lastRound, redeemed entries
    - grep shows zero `gas: 1_500_000n` remaining; three EXECUTE_ROUND_GAS_* constants + REDEEM_IOU_GAS exported with measurement-citing comments
    - Every write still sets maxFeePerGas: MIN_MAX_FEE_PER_GAS and explicit gas (no estimation path anywhere — Pitfall 8)
    - demo/setup.ts deploys with the four constructor args (uncalibrated defaults matching DeployV2.s.sol)
    - npx tsc --noEmit clean
  </acceptance_criteria>
  <done>The client speaks the new ABI with measured, size-aware explicit gas — the STATE.md hardcoded-limit concern is retired.</done>
</task>

<task type="auto">
  <name>Task 2: Reads, fetchManifest, and prepareRedemptionProofs</name>
  <files>src/client.ts</files>
  <read_first>
    - src/client.ts read pattern :32-45 (readContract shape + uint64 coercion)
    - .planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-RESEARCH.md — Code Example "TS: historical manifest reconstruction" and Anti-Pattern "Trusting the coordinator for historical leaf sets"
    - src/merkle.ts (nonInclusionProof/NonInclusionProof surface; EMPTY_MANIFEST_ROOT)
  </read_first>
  <action>
    Extend HubClient with reads copying the collateral() pattern: lastRound(addr): Promise<bigint> (BigInt-coerce uint64), redeemed(id): Promise<boolean>, rootRing(slot: bigint): Promise<{ root: Hex; nonce: bigint; executedAt: bigint }>, hashIou(iou: Iou): Promise<Hex>, plus ringSize(): Promise<bigint> reading the RING immutable getter. fetchManifest(nonce: bigint): Promise<Hex[]> — getContractEvents for RoundExecuted at that nonce, getTransaction(log.transactionHash), decodeFunctionData({ abi: clearingHubV2Abi, data: tx.input }), return args.consumedIds (signature-bound via the digest; NEVER a coordinator endpoint — doc comment states the zero-trust rationale). prepareRedemptionProofs(id: Hex): Promise<NonInclusionProof[]> — read roundNonce and RING, derive the buffered nonce range exactly as the contract does (ascending, count = min(roundNonce, RING)), fetchManifest per nonce, and for each: if the manifest is empty (sentinel root) push a structurally-valid placeholder proof (kind belowFirst with a zeroed InclusionProof — contract short-circuits sentinel roots without reading content), else build via nonInclusionProof(sortedIds, id); doc comment notes the TOCTOU property: if a round lands before the redemption mines, the contract's count/position check reverts and the caller simply regenerates.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All six new reads + fetchManifest + prepareRedemptionProofs exist with the exact names/signatures above
    - fetchManifest uses decodeFunctionData against clearingHubV2Abi (grep passes); no coordinator-endpoint fallback exists
    - prepareRedemptionProofs derives the range from on-chain roundNonce/RING reads, not from caller input
  </acceptance_criteria>
  <done>A creditor needs only an RPC endpoint to reconstruct any buffered round's leaf set and assemble a complete, contract-shaped proof array (MERK-03 tooling).</done>
</task>

<task type="auto">
  <name>Task 3: L-convention in iou.ts, redeemedIds in net(), call-site audit</name>
  <files>src/iou.ts, src/netting.ts, src/domain.ts, test/genFixture.ts, test/fixtures/digest.json, test/eip712.test.ts, test/rebuild.test.ts</files>
  <read_first>
    - src/iou.ts (full file — signIou signature with trailing chainId, the throw-with-interpolated-values precondition style at :39-41)
    - src/netting.ts :21-45 (net() opts shape with settledIds; the rule-1 dedup/filter site where redeemedIds joins)
    - .planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-RESEARCH.md — Critical Question 1 (the exact convention: expiry <= signTime + L; refuse to sign otherwise; {ok,reason} helper for verification paths)
    - test/genFixture.ts :30-37 (fixture IOU expiry is 4_102_444_800n — year 2100; the fixed `now` override keeping generation deterministic under the new check)
    - test/eip712.test.ts and test/rebuild.test.ts (audit every signIou call's expiry against now + 86400)
  </read_first>
  <action>
    src/domain.ts: export DEFAULT_MAX_IOU_LIFETIME_SECONDS = 86_400n with a doc comment labeling it UNCALIBRATED (matches the hub's L immutable; Phase-3 calibration). src/iou.ts: add checkIouLifetime(iou: Iou, opts: { now: bigint; maxIouLifetimeSeconds?: bigint }): { ok: boolean; reason?: string } returning ok:false with an interpolated reason when iou.expiry > opts.now + L (never throws — project check-function convention); extend signIou with a trailing opts?: { now?: bigint; maxIouLifetimeSeconds?: bigint } (after the existing chainId param), defaulting now to BigInt(Math.floor(Date.now() / 1000)); signIou throws Error (precondition style, interpolated values) when checkIouLifetime fails — the doc comment states the safety story: every consuming round then executes inside [expiry − L, expiry), which is what makes the hub's coverage rule complete for honest debtors; a debtor violating the convention weakens only their own double-claim protection. src/netting.ts: add optional redeemedIds?: ReadonlySet<Hex> to net() opts, filtered exactly like settledIds (lowercase-normalized union at the rule-1/settled filter site; update the numbered-rules doc comment to mention redeemed exclusion). Then audit and fix every signIou call site for the new check: test/genFixture.ts must pass an explicit fixed `now` (e.g. 4_102_444_800n - 86_400n) so the 2100 expiry passes deterministically — regenerate via `npm run fixture` and confirm digest.json is byte-identical (deterministic ECDSA: same message → same signature; if any value shifts, something is wrong — investigate, don't commit); demo/simulate.ts (1h expiry) and demo/e2e.ts (farExpiry = now + 86_400n, boundary-safe under <=) need no change but confirm by running the suites; fix any test in eip712.test.ts / rebuild.test.ts whose fixed expiry exceeds wall-clock now + 86400 by passing an explicit now/lifetime opt rather than weakening the default.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run fixture && git diff --exit-code test/fixtures/digest.json && npm test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - checkIouLifetime returns { ok, reason? } and never throws; signIou throws with interpolated expiry/now/L values on violation
    - net() accepts redeemedIds and excludes those ids from netting (existing settledIds tests still green; add one unit case in the touched test files if none covers it)
    - `npm run fixture` after the change leaves digest.json byte-identical (git diff --exit-code passes)
    - npm test fully green
  </acceptance_criteria>
  <done>D-15's SDK half (the L signing convention) and D-14's SDK half (redeemed-id exclusion from netting) are live with zero fixture drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK → Arc RPC | Writes must never rely on gas estimation (USDC gas-token gotcha) |
| creditor tooling → chain history | Leaf sets must come from signature-bound calldata, never a coordinator |
| IOU signer → future redemption window | Over-long expiries silently void the coverage guarantee |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-25 | DoS | gas estimation reserving the whole USDC balance on redeemIOU/executeRound | mitigate | Explicit measured-gas constants on every write; no estimation path (Pitfall 8) |
| T-02-26 | Spoofing | coordinator-served fake historical leaf sets | mitigate | fetchManifest decodes executeRound calldata only (signature-bound); doc-comment rationale |
| T-02-27 | Tampering | debtor signing expiry > signTime + L to reopen double-claim window | mitigate | signIou refuses; checkIouLifetime for verifiers; incentive analysis in doc comment (only the violator's own protection weakens) |
| T-02-28 | Tampering | stale/incomplete proof set from client-side range guessing | mitigate | prepareRedemptionProofs mirrors the contract's derived range; contract still the enforcer (Pitfall 5) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm test` green
- `npm run fixture` produces zero digest.json diff
- grep: no `gas: 1_500_000n`, no estimation-based write
</verification>

<success_criteria>
- MERK-03: full creditor tooling (reads, reconstruction, proof assembly, redeemIOU write) with measured explicit gas
- MERK-04/D-14: net() redeemedIds exclusion live
- D-15: L-convention enforced at signing with {ok,reason} verification helper
</success_criteria>

<output>
Create `.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-06-SUMMARY.md` when done
</output>
