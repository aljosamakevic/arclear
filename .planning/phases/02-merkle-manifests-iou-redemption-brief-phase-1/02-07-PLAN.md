---
phase: 02-merkle-manifests-iou-redemption-brief-phase-1
plan: 07
type: execute
wave: 6
depends_on: [02-06]
files_modified: [demo/coordinator.ts, demo/e2e.ts]
autonomous: true
requirements: [MERK-03, MERK-04]
must_haves:
  truths:
    - "The coordinator excludes redeemed IOU ids from every subsequent round: it reconciles IouRedeemed events from chain logs and passes redeemedIds into net() (D-14 off-chain half, D-09 counters untouched)"
    - "The e2e liveness scenario extends to full redemption (D-17): debtor dark past K EXECUTED rounds → creditor reconstructs proofs from calldata and redeems → debtor collateral debited to the base unit → the redeemed id never appears in any later round's consumed set"
    - "Phase-1 hardening is not regressed: screenConsents signature verification, pendingSubmission reconciliation, /round 409 guard, verifyProposal nonce/overlap pinning all intact"
  artifacts:
    - path: "demo/coordinator.ts"
      provides: "redeemedIds set + IouRedeemed reconciliation + net() opts wiring"
      contains: "IouRedeemed"
    - path: "demo/e2e.ts"
      provides: "On-chain-staleness redemption scenario with base-unit assertions"
      contains: "redeemIOU"
  key_links:
    - from: "demo/coordinator.ts"
      to: "IouRedeemed event logs"
      via: "getContractEvents reconciliation (pendingSubmission pattern)"
      pattern: "getContractEvents"
    - from: "demo/e2e.ts"
      to: "HubClient prepareRedemptionProofs + redeemIOU"
      via: "creditor recovery path"
      pattern: "prepareRedemptionProofs"
---

<objective>
Wire redemption into the reference coordinator and prove the whole story end-to-end on anvil: a creditor recovers from an unresponsive debtor using only chain data, and the redeemed IOU is structurally dead for netting forever after (D-17).

Purpose: MERK-03's demonstrable recovery path and MERK-04's exclusivity, observable in the running system. This is the phase's core acceptance scenario before docs + testnet redeploy close it out.
Output: extended demo/coordinator.ts and demo/e2e.ts; `npm run e2e:anvil` green including the new scenario.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-CONTEXT.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-RESEARCH.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-PATTERNS.md
@.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Coordinator redeemed-id reconciliation and filtering</name>
  <files>demo/coordinator.ts</files>
  <read_first>
    - demo/coordinator.ts (targeted: open-IOU filter :368, pendingSubmission getContractEvents reconciliation :382-400, settledIds lowercase-fold :549-550, applyMissSemantics :158-172 which must NOT be wired to redemption, round submission :477-493)
    - .planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-PATTERNS.md — "demo/coordinator.ts" section (exact slot-in points) and Pitfall 4 (never derive redemption state from miss counters)
    - src/client.ts (new fetchManifest/redeemed reads and the IouRedeemed event in clearingHubV2Abi — from plan 02-06)
    - src/netting.ts net() opts (redeemedIds param added in 02-06)
  </read_first>
  <action>
    Extend the Coordinator class: add a `redeemedIds` instance Set<Hex> (parallel to settledIds — keep them separate so the dashboard/report can distinguish settled-by-round from recovered-by-redemption). During the existing reconciliation step (the same place pendingSubmission folds via getContractEvents), also query IouRedeemed logs since the last-seen block and fold each event's id into redeemedIds as `id.toLowerCase() as Hex`. Pass `redeemedIds: this.redeemedIds` through net() opts wherever the coordinator nets (and mirror the exclusion in the open-IOU filter at :368 so redeemed paper drops from the candidate view immediately). Miss counters (applyMissSemantics) remain byte-untouched and are NEVER consulted for redemption state (D-09; Pitfall 4). The submission path needs no arg change (HubClient.executeRound method signature was preserved in 02-06). Do not modify screenConsents, pendingSubmission logic, or the /round 409 guard — the post-review hardening is regression-protected by the acceptance criteria.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Coordinator has a distinct redeemedIds set folded ONLY from confirmed IouRedeemed chain logs (grep: no assignment to redeemedIds outside the event-reconciliation path)
    - net() calls pass redeemedIds; the open-IOU filter excludes redeemed ids
    - git diff shows applyMissSemantics, screenConsents, and the pendingSubmission block unchanged
  </acceptance_criteria>
  <done>The coordinator's view converges with on-chain redemption state via logs alone, and redeemed paper can never re-enter a proposal it builds (D-14 off-chain half).</done>
</task>

<task type="auto">
  <name>Task 2: e2e redemption scenario (D-17)</name>
  <files>demo/e2e.ts</files>
  <read_first>
    - demo/e2e.ts (check() :25-29, the stall scenario structure :130-226 incl. per-pair nonce map, farExpiry, assertDeltas, consumed-manifest set-difference checks :193-199, disjointness check :221-224)
    - .planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-RESEARCH.md — Pitfall 4 (the e2e MUST drive the on-chain condition: K executed rounds without the staller, not coordinator counters)
    - src/client.ts (prepareRedemptionProofs, redeemIOU, redeemed, lastRound reads)
  </read_first>
  <action>
    Extend demo/e2e.ts with a redemption scenario after the existing exclusion scenario, reusing the check() idiom: (1) sign one IOU debtor=staller → creditor=another persona, amount fixed in base units, expiry within the L-convention (now + 86_400n is boundary-safe); (2) set staller.stalled = true and run at least K=3 EXECUTED rounds that settle among the other personas (generate fresh traffic between non-stalled personas per round so each round actually executes — an aborted round advances no on-chain clock; assert via hubClient.lastRound(staller) and roundNonce reads that the on-chain staleness condition roundNonce + 1 - lastRound >= 3 holds before redeeming); (3) creditor path: proofs = await hubClient.prepareRedemptionProofs(id), snapshot both collateral balances, submit hubClient.redeemIOU(creditorWallet, iou, sig, proofs), await receipt; (4) check(): debtor collateral debited by exactly iou.amount, creditor credited by exactly iou.amount, hubClient.redeemed(id) === true; (5) exclusivity tail: unstall the staller, run another full round with traffic touching the staller, and check() the redeemed id is absent from that round's consumed manifest (coordinator filter) AND remains absent across the union of all later consumed sets (reuse the set-disjointness idiom at :221-224) — the id can never settle (MERK-04/D-17). Keep every pre-existing scenario and assertion untouched.
  </action>
  <verify>
    <automated>npm run e2e:anvil 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - e2e output shows the new checks passing: staleness precondition, exact base-unit debit/credit, redeemed(id) true, id-absent-from-later-manifests
    - The staleness setup asserts the ON-CHAIN condition from lastRound/roundNonce reads (grep: no reference to coordinator miss counters in the new scenario)
    - `npm run e2e:anvil` exits 0 with zero failures across old + new scenarios
  </acceptance_criteria>
  <done>D-17 is demonstrably true on a live chain: dark debtor → creditor self-serve recovery from calldata-reconstructed proofs → permanent netting exclusion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinator view ↔ chain state | Redemption happens outside the coordinator; its view must converge from logs |
| e2e assertions → protocol claims | The doc claims in 02-08 lean on this scenario actually passing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-29 | Tampering | coordinator re-netting a redeemed IOU (state divergence) | mitigate | Log-driven redeemedIds fold + net() exclusion + on-chain NullifiedIdInManifest backstop (Task 1) |
| T-02-30 | Repudiation | e2e asserting eligibility from off-chain counters (false demo) | mitigate | Explicit on-chain staleness assertion from lastRound/roundNonce reads (Pitfall 4) |
| T-02-31 | Tampering | regression of Phase-1 consent-screening hardening | mitigate | Diff-scoped acceptance criteria: screenConsents/pendingSubmission/miss counters byte-untouched |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` clean; `npm test` green
- `npm run e2e:anvil` exits 0 including the redemption scenario
- Phase-1 hardened paths unchanged in the diff
</verification>

<success_criteria>
- D-17 end-to-end scenario green on anvil
- MERK-04 exclusivity observable in the running system (redeemed id never settles)
- Coordinator remains zero-authority: redemption state read from chain, miss counters demoted to early warning (D-06/D-09)
</success_criteria>

<output>
Create `.planning/phases/02-merkle-manifests-iou-redemption-brief-phase-1/02-07-SUMMARY.md` when done
</output>
