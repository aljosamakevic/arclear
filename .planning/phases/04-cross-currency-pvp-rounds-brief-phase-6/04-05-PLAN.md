---
phase: 04-cross-currency-pvp-rounds-brief-phase-6
plan: 05
type: execute
wave: 3
depends_on: [04-03, 04-04]
files_modified:
  - demo/setup.ts
  - .env.example
  - demo/agents.ts
  - demo/fx.ts
  - demo/pvp.ts
  - demo/coordinator.ts
  - test/pvpRound.test.ts
autonomous: true
requirements: [PVP-01, PVP-02]
tags: [demo, coordinator, orchestration, anvil, fx]

must_haves:
  truths:
    - "setupAnvil boots a dual-hub world: two mock tokens, two ClearingHubV2s, one PvPRouter bound to both, all agents funded and deposited on both hubs"
    - "Testnet mode reads HUB_V2_EURC and PVP_ROUTER env keys with actionable error messages"
    - "attemptPvPRound collects leg consents + PvPRound signatures from the union in ONE provider call per member, excludes non-responders from BOTH legs in one batch, rebuilds at unchanged per-hub nonces, and aborts cleanly after two passes (D-08, D-09)"
    - "While a PvP bundle is in flight, ordinary rounds are refused on both hubs; submission failure is classified from both chains' nonces, never error strings"
  artifacts:
    - path: "demo/setup.ts"
      provides: "dual-hub + router bootstrap (anvil) and HUB_V2_EURC/PVP_ROUTER env (testnet)"
      contains: "PVP_ROUTER"
    - path: "demo/fx.ts"
      provides: "FxQuote amount-pair mirror of the arc-stablecoin-fx App Kit quote shape + quoteToRate"
      contains: "amountIn"
    - path: "demo/pvp.ts"
      provides: "fxTradePair, attemptPvPRound (chain-free core), runPvPRound (chain-aware wrapper)"
      min_lines: 150
    - path: "test/pvpRound.test.ts"
      provides: "attemptPvPRound outcome tests with fake providers + injected submit"
      min_lines: 80
  key_links:
    - from: "demo/setup.ts"
      to: "src/abi/PvPRouter.ts"
      via: "pvpRouterBytecode deploy on anvil"
      pattern: "pvpRouterBytecode"
    - from: "demo/pvp.ts"
      to: "src/pvp.ts"
      via: "buildPvPProposal/verifyPvPConsent/unionParticipants"
      pattern: "buildPvPProposal"
    - from: "demo/pvp.ts"
      to: "demo/coordinator.ts"
      via: "reuses collectConsents/screenConsents/rebuildProposal machinery, gains no authority"
      pattern: "collectConsents|screenConsents"
---

<objective>
Make the demo world dual-hub and orchestrate the PvP bundle: extend `demo/setup.ts` (which today deploys ONE hub — the extension is real work, RESEARCH correction), mirror the arc-stablecoin-fx amount-pair quote shape (D-06 fallback), and build `attemptPvPRound` — the chain-free two-pass PvP consent state machine wrapping the existing Phase 1 machinery (D-07, D-08, D-09) — plus the chain-aware submit/reconcile wrapper.

Purpose: PVP-01/PVP-02 need a working coordinator path before the e2e scenario (plan 04-06) can prove both-or-neither on anvil.
Output: dual-hub bootstrap, FX quote mirror, PvP orchestration with unit-tested abort semantics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-cross-currency-pvp-rounds-brief-phase-6/04-CONTEXT.md
@.planning/phases/04-cross-currency-pvp-rounds-brief-phase-6/04-RESEARCH.md

<interfaces>
From demo/coordinator.ts (existing — reuse, do not fork):
```typescript
export type ConsentOutcome = { kind: "consent"; signature: Hex } | { kind: "refusal"; reason: string };
export type ConsentProvider = (proposal: RoundProposal, excluded: Address[]) => Promise<ConsentOutcome>;
export async function collectConsents(proposal, excluded, providers: Map<string, ConsentProvider>, windowMs): Promise<ConsentCollection>;
export async function screenConsents(hub, proposal, collection, chainId?): Promise<ConsentCollection>;
export async function attemptRound(args: { ious, expectedRoundNonce, providers, windowMs, submit, ... }): Promise<RoundAttemptOutcome>;
// RoundAttemptOutcome = { kind: "settled" | "aborted" | "empty", ... }
// Coordinator.pendingSubmission: { roundNonce, digest, consumedIds, sentAtBlock, txHash? } — WR-01/WR-02 pattern
```

From src/pvp.ts (plan 04-01): `buildPvPProposal`, `verifyPvPProposal`, `signPvPConsent`, `verifyPvPConsent`, `unionParticipants`, `rateConsistent`, `pvpDigest`.
From src/round.ts: `rebuildProposal(proposal, ious, excluded, opts)` — rebuild at unchanged nonce.
From src/client.ts (plan 04-04): `PvPRouterClient` with `executePvP(wallet, proposal, legSignatures, pvpSignatures)`, plus `HubClient.roundNonce()`.
From src/abi/PvPRouter.ts (plan 04-04): `pvpRouterAbi`, `pvpRouterBytecode`.
From demo/setup.ts (existing): `DemoEnv` interface, `setupAnvil()`, `setupTestnet()`, `depositAll` helper.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dual-hub + router bootstrap (anvil and testnet) + FX personas</name>
  <files>demo/setup.ts, .env.example, demo/agents.ts</files>
  <read_first>demo/setup.ts (whole file — setupAnvil deploy sequence at 80-130, depositAll at 42-77, testnet env guards at 139-145, DemoEnv at 20-30), demo/agents.ts lines 20-31 (persona shape), 04-RESEARCH.md Pitfall 3 (single-hub assumptions)</read_first>
  <action>
    demo/setup.ts anvil path: after the existing token+hub deploy, deploy a second mock token (the EURC stand-in) and a second ClearingHubV2 with the identical UNCALIBRATED args, then deploy PvPRouter via `pvpRouterBytecode` with `args: [hubUsdc, hubEurc]`; mint both tokens to every agent and deposit collateral on BOTH hubs (parameterize or call the depositAll helper per hub — Pitfall 3: keep per-hub state strictly separate). Extend `DemoEnv` with `tokenEurc`, `hubEurc`, `hubClientEurc`, `router`, `routerClient` (PvPRouterClient) — per-field doc comments. All deploy/deposit writes keep explicit gas per house rules.

    Testnet path: read `HUB_V2_EURC` and `PVP_ROUTER` from env with the same actionable-error-message guard style as HUB_V2_USDC ("PVP_ROUTER not set — deploy PvPRouter first (see README)"); construct hubClientEurc/routerClient from them. Add `PVP_ROUTER=` to .env.example under the V2 section with a one-line comment.

    demo/agents.ts: update two personas' role text to make them the designated EURC↔USDC FX traders (Claude's discretion on flavor — e.g., one pays for EU-sourced data in EURC, receives USDC); keep index 0 reserved for deployer/relayer and keep the `stalled` flag untouched.
  </action>
  <verify>
    <automated>npx tsx -e "import('./demo/setup.js').catch(()=>0)" ; npx tsx --tsconfig tsconfig.json -e "const {setup}=await import('./demo/setup.ts'); const env=await setup('anvil'); const hu=await env.routerClient.hubUSDC(); const he=await env.routerClient.hubEURC(); if(hu.toLowerCase()!==env.hub.toLowerCase()||he.toLowerCase()!==env.hubEurc.toLowerCase()){env.anvil?.kill();process.exit(1)}; env.anvil?.kill(); console.log('dual-hub bootstrap OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The smoke script prints "dual-hub bootstrap OK": router immutables read back on-chain match the two deployed hub addresses
    - Every agent has nonzero deposited collateral on BOTH hubs after setupAnvil (assert inside the smoke or via hubClientEurc.collateral reads)
    - `grep -c 'PVP_ROUTER' .env.example demo/setup.ts` shows the key in both; testnet guard messages name the missing key and the fix
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The demo environment is genuinely dual-hub with a bound router, on anvil and testnet configs, with per-hub state kept separate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FX quote mirror + attemptPvPRound chain-free core</name>
  <files>demo/fx.ts, demo/pvp.ts, test/pvpRound.test.ts</files>
  <read_first>demo/coordinator.ts lines 29-50 (ConsentOutcome/Provider types), 54-130 (collectConsents deadline-snapshot semantics), 132-160 (screenConsents), 178-305 (attemptRound — the structural template: aborts as data, one-batch exclusion, rebuild at same nonce, 2-pass cap, injected submit), 04-RESEARCH.md Pattern 3 + Pattern 4</read_first>
  <behavior>
    - All union members consent → outcome "settled", submit called exactly once with complete leg + PvP signature sets
    - One union member times out in pass 1 → excluded from BOTH legs, both legs rebuilt at unchanged nonces, pass 2 settles without them
    - A pass-2 refusal → outcome "aborted", submit never called
    - Either leg nets to fewer than 2 participants (before or after exclusion) → outcome "empty"/aborted-as-data, submit never called
    - An FX pair whose amounts violate the quoted rate → the honest member's verifyPvPProposal rejects → refusal recorded
  </behavior>
  <action>
    demo/fx.ts: `FxQuote` interface `{ amountIn: bigint; amountOut: bigint; timestamp: number }` — doc comment stating this mirrors the arc-stablecoin-fx sample's App Kit `estimateSwap` amount-pair quote shape and that mirroring the shape is the D-06-sanctioned fallback (sample itself is a Next.js/Supabase app unreachable from a viem-only SDK); `quoteToRate(q): { fxNumerator: bigint; fxDenominator: bigint }` (amountOut/amountIn — EURC per USDC base units, no division anywhere); `sampleQuote(seed?)` returning a deterministic demo quote near 0.989589 EURC/USDC in 6-decimal base units.

    demo/pvp.ts: `fxTradePair(a: {account, address}, b, usdcAmount: bigint, quote: FxQuote, opts: {nonces, expiry}): Promise<{usdc: SignedIou, eurc: SignedIou}>` — builds and signs the two opposite-direction IOUs with a shared `ref` (keccak of a trade id) and cross-multiplication-exact amounts against the correct hub domains.

    `attemptPvPRound(args: { usdc: { ious: SignedIou[]; hub: Address; expectedRoundNonce: bigint; settledIds?: Set<string> }; eurc: { ...same }; router: Address; fxNumerator: bigint; fxDenominator: bigint; providers: Map<string, PvPConsentProvider>; windowMs: number; submit: (proposal: PvPProposal, legSigs: { usdc: Hex[]; eurc: Hex[] }, pvpSigs: Hex[]) => Promise<Hex>; chainId?: number }): Promise<PvPAttemptOutcome>` where `PvPConsentProvider` returns `{ kind: "consent"; usdcConsent?: Hex; eurcConsent?: Hex; pvpSignature: Hex } | { kind: "refusal"; reason: string }` — ONE provider call per union member per pass (D-08 fold: leg consent(s) + PvPRound signature in a single verified step). Outcome union `PvPAttemptOutcome = { kind: "settled"; ... } | { kind: "aborted"; reason: string; ... } | { kind: "empty" }` — aborts as data, never throws (house convention).

    Flow (mirror attemptRound's shape): net() + buildProposal per leg (empty/thin legs → "empty"); buildPvPProposal; pass-1 collection over the union with one wall-clock deadline (reuse or generalize collectConsents' snapshot semantics — microtask routing, throwing provider → reasoned refusal); screening: leg consents via verifyConsent per hub, pvpSignature via verifyPvPConsent — any invalid demoted to refusal; any missing/refused union member → exclude them from BOTH legs in one batch (D-09 simplest safe rule), rebuild both legs via rebuildProposal at unchanged per-hub nonces, rebuild the PvPProposal (digests changed transitively), full recollection in pass 2 — include the why-comment noting the unchanged-leg-consent optimization is deliberately skipped (RESEARCH open question 2); hard 2-pass cap; pass-2 incompleteness → "aborted", nothing submitted. On success call submit exactly once.

    test/pvpRound.test.ts: cover the behavior list with fake providers (deterministic accounts from the eip712 test idiom) and a recording fake submit. No anvil, no network — the chain-free core is the point (RESEARCH open question 1 resolution).
  </action>
  <verify>
    <automated>npx vitest run test/pvpRound.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/pvpRound.test.ts` exits 0 with ≥5 tests matching the behavior list
    - attemptPvPRound never throws for any tested input — all failures are outcome data
    - Exclusion test asserts the excluded address appears in NEITHER rebuilt leg's participants and that both rebuilt legs kept their original nonces
    - `grep -c 'quoteToRate\|amountIn' demo/fx.ts` ≥2 and the D-06 fallback doc comment is present (grep 'arc-stablecoin-fx' demo/fx.ts)
  </acceptance_criteria>
  <done>The two-pass PvP consent state machine works and is unit-proven without a chain, honoring D-07/D-08/D-09 exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Chain-aware runPvPRound wrapper + coordinator in-flight guard</name>
  <files>demo/pvp.ts, demo/coordinator.ts, test/pvpRound.test.ts</files>
  <read_first>demo/coordinator.ts lines 308-360 (RunRoundResult, ExecutedRound, Coordinator fields), 420-465 (reconcilePendingSubmission — WR-01/WR-02: record before broadcast, classify from chain state, blocked result), 540-575 (submit path), 04-RESEARCH.md Pitfall 4</read_first>
  <behavior>
    - runPvPRound records pending state for BOTH hubs before broadcasting; on a reverted/failed submission it classifies from both on-chain nonces (moved nonce = expected concurrency, returned as data)
    - While a PvP bundle is in flight, Coordinator.runRound on either hub returns a blocked result (no ordinary round may advance either nonce — Pitfall 4)
    - A settled PvP round appears in both coordinators' rounds lists tagged as PvP with the fx rate, and each hub's settledIds absorbs its own leg's consumedIds (per-hub, never shared — Pitfall 3)
  </behavior>
  <action>
    demo/pvp.ts: `runPvPRound(deps)` chain-aware wrapper taking injected deps (two HubClient-shaped readers, a PvPRouterClient-shaped submitter, relayer wallet, the two Coordinators or their state surfaces) so it is unit-testable with fakes. Flow: read both roundNonces; get quote via demo/fx.ts; call attemptPvPRound with submit = routerClient.executePvP; before broadcast record a pending record per hub `{ roundNonce, digest, consumedIds, sentAtBlock }` (WR-01 generalized); after the receipt, classify from chain state — both nonces advanced by 1 = settled; either nonce moved unexpectedly = concurrency, return blocked/aborted as data; never match error strings (WR-02). On settle: push a PvP-tagged `ExecutedRound` entry (add optional field `pvp?: { fxNumerator: string; fxDenominator: string }` to ExecutedRound) into BOTH coordinators' rounds and mark each hub's settledIds from its own leg.

    demo/coordinator.ts: add a minimal external-hold mechanism (e.g. a public `hold(reason)/release()` pair or an injected `isHeld` check consulted at the top of runRound returning the existing blocked-style result) so the PvP wrapper can freeze ordinary rounds on both hubs while a bundle is in flight. Keep the coordinator zero-authority: it still only assembles and relays signatures (PROJECT constraint). Do not add any new HTTP endpoint; the PvP tag flows through the existing /state serialization of rounds (D-12).

    test/pvpRound.test.ts: add wrapper tests with fake clients — assert pending-before-broadcast ordering (fake submit checks pending is already set), nonce-based classification (fake nonce reader simulating a concurrent advance → blocked outcome), hold blocking ordinary runRound, and the PvP tag + per-hub settledIds after a settled run.
  </action>
  <verify>
    <automated>npx vitest run test/pvpRound.test.ts && npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Wrapper tests pass: pending recorded before broadcast, classification from nonces only (`grep -c 'message.includes\|toMatch(/Wrong' demo/pvp.ts` shows no error-string classification), hold blocks ordinary rounds
    - ExecutedRound gains the optional pvp field; no new server endpoints (`git diff demo/server.ts` empty or server untouched)
    - `npm test` fully green — coordinator regression suites unaffected
  </acceptance_criteria>
  <done>The PvP submit path carries the WR-01/WR-02 discipline across both hubs and the demo surface stays within the D-12 bound.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinator → members | Coordinator assembles bundles but holds no keys; every member re-verifies via verifyPvPProposal before signing |
| coordinator → chain | The only party ever holding a complete leg signature set pre-broadcast; custody discipline applies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | Elevation | coordinator authority | mitigate | zero-authority preserved: coordinator cannot forge any signature; members sign only what they verified (PROJECT hard constraint) |
| T-04-20 | Information disclosure | leg signature custody | mitigate | leg signatures published ONLY inside the router tx submit; providers sign only through the verified-bundle flow (RESEARCH Q2c hardening i/ii) |
| T-04-21 | DoS | nonce race across the bundle | mitigate | in-flight hold on both hubs + WR-01 pending records + WR-02 chain-state classification (Pitfall 4) |
| T-04-22 | Tampering | rate manipulation by coordinator | mitigate | every union member independently cross-multiplies FX pairs in verifyPvPProposal before signing — a wrong rate never collects signatures |
| T-04-23 | Repudiation | cross-hub consent reuse | mitigate | per-hub settledIds/pending state kept strictly separate; ids are hub-domain-separated by construction (Pitfall 3) |
| T-04-SC | Tampering | package installs | accept | no new packages this phase |
</threat_model>

<verification>
- `npm test && npx tsc --noEmit` green; dual-hub bootstrap smoke passes on anvil
</verification>

<success_criteria>
- The demo can build, verify, collect, and submit a PvP bundle end-to-end against the anvil dual-hub world, with abort semantics unit-proven (D-07/D-08/D-09)
- FX rate sourced in the arc-stablecoin-fx amount-pair shape with the D-06 fallback documented in code
</success_criteria>

<output>
Create `.planning/phases/04-cross-currency-pvp-rounds-brief-phase-6/04-05-SUMMARY.md` when done.
</output>
