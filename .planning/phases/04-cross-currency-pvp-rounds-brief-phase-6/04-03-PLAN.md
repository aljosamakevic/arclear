---
phase: 04-cross-currency-pvp-rounds-brief-phase-6
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - test/genFixture.ts
  - test/fixtures/digest.json
  - test/pvp.test.ts
  - contracts/test/PvPParity.t.sol
autonomous: true
requirements: [PVP-02]
tags: [fixtures, eip712, parity, foundry, testing]

must_haves:
  truths:
    - "TS pvpDigest and Solidity hashPvPRound produce the byte-identical digest for the shared fixture vector (D-05 — mandatory)"
    - "A PvPRound consent signed in TS recovers to the correct signer via OZ ECDSA on-chain"
    - "Regenerating the fixture (npm run fixture) changes no pre-existing key — v1/v2 Round and IOU parity vectors are untouched"
  artifacts:
    - path: "test/genFixture.ts"
      provides: "pvp_* fixture keys (pvpRouter, pvpHubUsdc, pvpHubEurc, leg digests, rate, pvpDigest, pvpSigner0, pvpConsent0)"
      contains: "pvpDigest"
    - path: "test/fixtures/digest.json"
      provides: "regenerated fixture with pvp keys"
      contains: "pvpRouter"
    - path: "contracts/test/PvPParity.t.sol"
      provides: "deployCodeTo parity test asserting digest + recovery equality"
      min_lines: 40
  key_links:
    - from: "contracts/test/PvPParity.t.sol"
      to: "test/fixtures/digest.json"
      via: "vm.readFile + vm.parseJson* (fs_permissions already granted)"
      pattern: "readFile.*digest\\.json"
    - from: "contracts/test/PvPParity.t.sol"
      to: "contracts/src/PvPRouter.sol"
      via: "deployCodeTo with fixture hub addresses as constructor args (immutables)"
      pattern: "deployCodeTo"
    - from: "test/pvp.test.ts"
      to: "test/fixtures/digest.json"
      via: "TS-side fixture lock (same vector both stacks)"
      pattern: "digest\\.json"
---

<objective>
Honor the mandatory D-05 fixture obligation for the new signed struct: extend `test/genFixture.ts` with pvp_* keys, regenerate `test/fixtures/digest.json`, lock the TS side with a vitest fixture test, and prove TS↔Solidity parity in `contracts/test/PvPParity.t.sol` via the twice-proven deployCodeTo-with-immutables pattern.

Purpose: Pitfall 5 — a typehash/domain drift between src/pvp.ts and PvPRouter.sol would otherwise surface only as on-chain BadPvPSignature. The fixture makes drift impossible to miss.
Output: extended fixture pipeline + PvPParity test, both green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-cross-currency-pvp-rounds-brief-phase-6/04-CONTEXT.md
@.planning/phases/04-cross-currency-pvp-rounds-brief-phase-6/04-RESEARCH.md

<interfaces>
<!-- CANONICAL typehash — already implemented identically in 04-01 (src/domain.ts PVP_TYPES) and 04-02 (PvPRouter.sol): -->
```
PvPRound(bytes32 usdcLegDigest,bytes32 eurcLegDigest,uint256 fxNumerator,uint256 fxDenominator)
```
Domain: `("ArclearPvPRouter", "1", chainId=5042002, verifyingContract=pvpRouter fixture address)`.

From src/pvp.ts (plan 04-01): `pvpDigest(router, {usdcLegDigest, eurcLegDigest, fxNumerator, fxDenominator}, chainId?)`, `signPvPConsent(router, proposal, account, chainId?)`, `buildPvPProposal(router, usdcLeg, eurcLeg, fxNumerator, fxDenominator, chainId?)`.

From contracts/src/PvPRouter.sol (plan 04-02): `constructor(ClearingHubV2 hubUSDC_, ClearingHubV2 hubEURC_)`, `hashPvPRound(bytes32,bytes32,uint256,uint256) public view returns (bytes32)`.

Fixture note: the router constructor only STORES the hub addresses — no hub code needs to exist at the fixture hub addresses for hashPvPRound parity (it touches no hub).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: genFixture pvp_* keys + regeneration + TS fixture lock</name>
  <files>test/genFixture.ts, test/fixtures/digest.json, test/pvp.test.ts</files>
  <read_first>test/genFixture.ts (whole file — deterministic keys, sorted accounts, flat-key fixture object, String() bigint serialization, "never hand-edit" header), test/eip712.test.ts lines 134-148 (TS fixture-lock test idiom), 04-RESEARCH.md Pattern 1 "Fixture fields needed"</read_first>
  <action>
    Extend test/genFixture.ts (regeneration-only discipline — never hand-edit digest.json): define fixture constants `PVP_HUB_USDC` (reuse the existing HUB constant address), `PVP_HUB_EURC` (a new all-2s-style constant address), `PVP_ROUTER` (a new all-3s-style constant address). Build two small leg proposals with the existing `buildProposal` machinery against PVP_HUB_USDC and PVP_HUB_EURC (reuse the fixture's deterministic signed IOUs / accounts; the EURC leg may reuse the same participant trio with distinct IOU nonces so its digest differs). Rate vector mirrors the App Kit amount-pair example: fxNumerator "989589", fxDenominator "1000000" (RESEARCH Pattern 4). Compute `pvpDigest` via buildPvPProposal and sign `pvpConsent0` with the sorted-first account via signPvPConsent.

    Add flat keys to the fixture object (bigints stringified via String(), matching the existing deltas.map(String) convention): `pvpHubUsdc`, `pvpHubEurc`, `pvpRouter`, `pvpUsdcLegDigest`, `pvpEurcLegDigest`, `pvpFxNumerator`, `pvpFxDenominator`, `pvpDigest`, `pvpSigner0`, `pvpConsent0`.

    Run `npm run fixture` to regenerate. Confirm via `git diff test/fixtures/digest.json` that ONLY additions appear — every pre-existing key/value byte-identical (deterministic generation guarantees this; a changed existing key means a regression and must halt the task).

    Add a fixture-lock test to test/pvp.test.ts (eip712.test.ts:134-148 idiom): read digest.json, recompute pvpDigest from the fixture's leg digests + rate at chainId 5042002 with the fixture router address, assert equality with `pvpDigest`, and assert `verifyPvPConsent` returns true for (pvpSigner0, pvpConsent0).
  </action>
  <verify>
    <automated>npm run fixture && git diff --stat test/fixtures/digest.json && npx vitest run test/pvp.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const f=require('./test/fixtures/digest.json'); for (const k of ['pvpHubUsdc','pvpHubEurc','pvpRouter','pvpUsdcLegDigest','pvpEurcLegDigest','pvpFxNumerator','pvpFxDenominator','pvpDigest','pvpSigner0','pvpConsent0']) if (!(k in f)) { console.error('missing '+k); process.exit(1) }"` exits 0
    - `git diff test/fixtures/digest.json` shows only added lines for pre-existing content (no modified existing keys)
    - `npx vitest run test/pvp.test.ts` exits 0 including the new fixture-lock test
    - `npm test` exits 0 (existing eip712/merkle fixture tests still green against the regenerated file)
  </acceptance_criteria>
  <done>digest.json carries the complete PvPRound vector, generated (never hand-edited), with the TS side locked to it.</done>
</task>

<task type="auto">
  <name>Task 2: PvPParity.t.sol — Solidity side of the D-05 lock</name>
  <files>contracts/test/PvPParity.t.sol</files>
  <read_first>contracts/test/ClearingHubV2Parity.t.sol (whole file — vm.readFile, vm.parseJsonAddress/Uint/Bytes32, vm.chainId, deployCodeTo with abi.encode'd constructor args), contracts/test/MerkleParity.t.sol lines 20-30 (flat-key string.concat addressing, if needed)</read_first>
  <action>
    Create contracts/test/PvPParity.t.sol: `contract PvPParityTest is Test`. setUp: `vm.readFile("../test/fixtures/digest.json")` (fs_permissions already grant this), parse `pvpRouter`, `pvpHubUsdc`, `pvpHubEurc`, `chainId`; `vm.chainId(chainId)`; `deployCodeTo("PvPRouter.sol:PvPRouter", abi.encode(pvpHubUsdcAddr, pvpHubEurcAddr), pvpRouterAddr)` — immutables set correctly under deployCodeTo (pattern proven by ClearingHubV2Parity's K/RING). Note in a comment: no hub code needed at the fixture hub addresses — hashPvPRound never calls them.

    Two assertions, mirroring ClearingHubV2Parity: (1) `router.hashPvPRound(pvpUsdcLegDigest, pvpEurcLegDigest, pvpFxNumerator, pvpFxDenominator)` equals the fixture `pvpDigest` — failure message must name D-05; (2) `ECDSA.recover(pvpDigest, pvpConsent0)` equals `pvpSigner0`. Parse the uint rate fields with vm.parseJsonUint against the stringified fixture values.
  </action>
  <verify>
    <automated>cd contracts && forge test --match-contract PvPParityTest -vvv</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-contract PvPParityTest` exits 0 with both assertions passing
    - Test reads ONLY test/fixtures/digest.json for vectors (no hardcoded digests in the .sol file — grep shows no 0x-64-hex literals besides none)
    - `npm run test:contracts` exits 0
  </acceptance_criteria>
  <done>TS and Solidity PvPRound digests are locked to one shared vector; typehash/domain drift now breaks CI-visible tests on both stacks (D-05 satisfied).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TS signer ↔ Solidity verifier | Cross-stack digest equality is the security foundation — a silent divergence would make honest consents unverifiable (DoS) or, combined with a bug, verify the wrong message |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering | PvPRound encoding drift | mitigate | shared fixture + dual assertions (digest equality, ECDSA recovery) — the D-05 mandatory pipeline |
| T-04-13 | Tampering | hand-edited fixtures | mitigate | regeneration-only discipline; git diff gate proves pre-existing keys untouched |
| T-04-14 | Spoofing | signature recovery parity | mitigate | ECDSA.recover on-chain vs viem verifyTypedData off-chain over the same consent bytes |
| T-04-SC | Tampering | package installs | accept | no new packages this phase |
</threat_model>

<verification>
- `npm run fixture && npm test && npm run test:contracts` all green in sequence (regenerate before parity runs — the phase sampling rule)
</verification>

<success_criteria>
- PVP-02's digest is provably identical across both implementations (D-05, the project's hard constraint for every new signed struct)
</success_criteria>

<output>
Create `.planning/phases/04-cross-currency-pvp-rounds-brief-phase-6/04-03-SUMMARY.md` when done.
</output>
