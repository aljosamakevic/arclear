---
phase: 04-cross-currency-pvp-rounds-brief-phase-6
plan: 04
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - contracts/test/utils/PvPRoundBuilder.sol
  - contracts/test/PvPRouter.t.sol
  - contracts/.gas-snapshot
  - src/abi/PvPRouter.ts
  - src/client.ts
autonomous: true
requirements: [PVP-01]
tags: [foundry, testing, gas, revert-matrix, viem-client]

must_haves:
  truths:
    - "A full PvP round settles both legs in one tx: both hub nonces advance, collateral deltas exact on both hubs, PvPExecuted emitted"
    - "Every failure mode reverts EVERYTHING: bad leg sig, wrong leg nonce, insufficient collateral on the second leg, paused hub, bad PvP sig, union count/order violations, leg-digest mismatch — after each revert, both nonces and all collateral are unchanged"
    - "Single-leg direct submission outside the router SETTLES — machine-documented limitation with the accept-and-document rationale (RESEARCH Q2c)"
    - "executePvP gas is measured at demo scale and the client formula carries the measured constants with provenance comments"
  artifacts:
    - path: "contracts/test/utils/PvPRoundBuilder.sol"
      provides: "dual-hub + router harness (hub-parameterized digest/sign/fund helpers, union sig builder)"
      min_lines: 80
    - path: "contracts/test/PvPRouter.t.sol"
      provides: "positive tests + full revert matrix + single-leg limitation test + gas tests"
      contains: "test_singleLegDirectSubmissionSettles"
    - path: "src/client.ts"
      provides: "PvPRouterClient.executePvP with formula gas + PVP_ROUTER_GAS_BASE / PVP_GAS_PER_UNION_SIG constants"
      contains: "PvPRouterClient"
    - path: "src/abi/PvPRouter.ts"
      provides: "pvpRouterAbi + pvpRouterBytecode"
      contains: "pvpRouterAbi"
  key_links:
    - from: "contracts/test/PvPRouter.t.sol"
      to: "contracts/test/utils/PvPRoundBuilder.sol"
      via: "inheritance"
      pattern: "is PvPRoundBuilder"
    - from: "src/client.ts"
      to: "src/abi/PvPRouter.ts"
      via: "abi import for read/write calls"
      pattern: "pvpRouterAbi"
    - from: "src/client.ts"
      to: "contracts/.gas-snapshot"
      via: "measured constants cited in provenance comments"
      pattern: "PVP_ROUTER_GAS_BASE"
---

<objective>
Prove PVP-01 exhaustively in Foundry — both-or-neither positively and against the full revert matrix, machine-document the single-leg limitation, measure executePvP gas — then wire the viem client (`PvPRouterClient`) with the measured formula so every Arc write carries an explicit gas limit.

Purpose: the router's atomicity claim is only real once a failing second leg is shown to revert the first leg's state; the single-leg test makes the accepted limitation honest and permanent.
Output: dual-hub harness, complete PvPRouter.t.sol suite, gas snapshot, abi module, client.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-cross-currency-pvp-rounds-brief-phase-6/04-CONTEXT.md
@.planning/phases/04-cross-currency-pvp-rounds-brief-phase-6/04-RESEARCH.md

<interfaces>
From contracts/src/PvPRouter.sol (plan 04-02):
```solidity
struct Leg { uint64 nonce; address[] participants; int256[] deltas; bytes32[] consumedIds; bytes[] signatures; }
function executePvP(Leg calldata usdcLeg, Leg calldata eurcLeg, bytes32 usdcLegDigest, bytes32 eurcLegDigest,
    uint256 fxNumerator, uint256 fxDenominator, bytes[] calldata pvpSignatures) external;
function hashPvPRound(bytes32, bytes32, uint256, uint256) public view returns (bytes32);
// errors: ZeroRate(), LegDigestMismatch(uint8), BadPvPSignature(uint256),
//         PvPSignatureCountMismatch(uint256,uint256), UnionNotStrictlyAscending()
// event: PvPExecuted(bytes32,bytes32,uint256,uint256,bytes32)
```

From src/client.ts (existing — reuse coefficients, copy the write shape):
```typescript
export const EXECUTE_ROUND_GAS_BASE = 300_000n;
export const EXECUTE_ROUND_GAS_PER_PARTICIPANT = 40_000n;
export const EXECUTE_ROUND_GAS_PER_ID = 6_000n;
// HubClient.executeRound: writeContract with { gas: <formula>, maxFeePerGas: MIN_MAX_FEE_PER_GAS } — never estimation
```

From src/types.ts (plan 04-01): `PvPProposal { usdcLeg: RoundProposal; eurcLeg: RoundProposal; fxNumerator: bigint; fxDenominator: bigint; digest: Hex }`.
From src/abi/ClearingHubV2.ts (existing): exports `clearingHubV2Abi`, `clearingHubV2Bytecode` — mirror this module shape from `contracts/out/PvPRouter.sol/PvPRouter.json`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PvPRoundBuilder dual-hub harness + positive both-legs-settle tests</name>
  <files>contracts/test/utils/PvPRoundBuilder.sol, contracts/test/PvPRouter.t.sol</files>
  <read_first>contracts/test/utils/RoundBuilderV2.sol (whole file — actor derivation/sorting at 31-54, _fundAndDeposit at 56-62, _manifest at 88, _digestV2 at 113, _signRound at 122, _buildSignatures at 135-145, _simpleRound at 99-109), contracts/test/PvPRouter.t.sol (plan 04-02's smoke setUp to migrate)</read_first>
  <action>
    Create contracts/test/utils/PvPRoundBuilder.sol: `abstract contract PvPRoundBuilder is Test` owning two mock tokens, two ClearingHubV2 instances (same UNCALIBRATED constructor args as RoundBuilderV2), and one PvPRouter. Port RoundBuilderV2's helpers parameterized by hub/token: `_digestV2(ClearingHubV2 hub, ...)`, `_signRound(uint256 pk, ClearingHubV2 hub, ...)`, `_buildSignatures(ClearingHubV2 hub, ...)`, `_fundAndDeposit(ClearingHubV2 hub, MockToken token, address actor, uint256 amount)`, `_manifest`. Add PvP-specific helpers: `_union(address[] a, address[] b)` sorted merge, `_signPvP(uint256 pk, bytes32 pvpDigest)`, `_buildPvPSignatures(address[] union_, bytes32 pvpDigest)` (index-aligned, keyed via the shared actor-key lookup), `_buildLeg(ClearingHubV2 hub, uint64 nonce, address[] p, int256[] d, bytes32[] ids)` returning a fully signed `PvPRouter.Leg`, and `_simplePvP(bytes32 salt)` producing a canonical bundle: USDC leg over actors {A,B,C}, EURC leg over actors {B,C,D} (overlapping union of 4 — differing sets are the NORMAL case per RESEARCH Q5), distinct manifests, a fixed rate pair (e.g. 989589/1000000).

    Migrate PvPRouter.t.sol's smoke setUp onto the harness (`contract PvPRouterTest is PvPRoundBuilder`), keeping the plan 04-02 tests passing. Add positive tests: `test_executePvP_bothLegsSettle` — record both roundNonces and all actors' collateral on both hubs, execute, assert both nonces +1, every collateral delta exactly matches the signed leg deltas on its hub, and PvPExecuted emitted with the right digests/rate (vm.expectEmit); `test_executePvP_disjointSets` — legs with disjoint participant sets (union = concatenation) settle; `test_executePvP_identicalSets` — identical sets settle with union == set.
  </action>
  <verify>
    <automated>cd contracts && forge test --match-contract PvPRouterTest -vvv</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --match-contract PvPRouterTest` exits 0; positive tests assert BOTH nonce advances AND per-actor collateral deltas on BOTH hubs (not just success)
    - Overlapping, disjoint, and identical participant-set cases all settle (RESEARCH Q5 union rule exercised in all three regimes)
    - Plan 04-02's smoke tests still pass unmodified in behavior
  </acceptance_criteria>
  <done>The canonical positive path is proven end-to-end in Foundry over a reusable dual-hub harness.</done>
</task>

<task type="auto">
  <name>Task 2: Full revert matrix + single-leg documented-limitation test</name>
  <files>contracts/test/PvPRouter.t.sol</files>
  <read_first>contracts/test/ClearingHubV2.t.sol lines 60-200 (revert-matrix idioms: typed selectors, vm.prank, pause paths), 04-RESEARCH.md "Code Examples" (both-or-neither assertion skeleton) and Q2c (the exact accept-and-document framing to cite)</read_first>
  <action>
    Extend PvPRouterTest with the revert matrix — EVERY test asserts the both-or-neither postcondition after the revert (both roundNonces unchanged, a representative actor's collateral unchanged on both hubs):
    - `test_revert_executePvP_badLegSignature`: corrupt one EURC leg signature → expect ClearingHubV2.BadSignature bubbled through the router; USDC leg (executed first) must be fully reverted — this is THE atomicity proof (Pitfall 1)
    - `test_revert_executePvP_wrongLegNonce`: stale nonce on one leg → WrongRoundNonce bubbles
    - `test_revert_executePvP_insufficientCollateralSecondLeg`: drain a debtor's collateral on the EURC hub only (via withdraw) → InsufficientCollateral bubbles, USDC leg reverted
    - `test_revert_executePvP_pausedHub`: pause the EURC hub (owner prank) → EnforcedPause bubbles (RESEARCH Q1.4)
    - `test_revert_executePvP_badPvPSignature`: tamper pvpSignatures[1] → BadPvPSignature(1)
    - `test_revert_executePvP_pvpSignatureCountMismatch`: drop one union signature → PvPSignatureCountMismatch
    - `test_revert_executePvP_unionDisorder`: swap two participants in one leg's array (breaking ascent) → UnionNotStrictlyAscending (router-local, before hub calls)
    - `test_revert_executePvP_legDigestMismatch`: pass a wrong usdcLegDigest → LegDigestMismatch(0); same for leg 1
    - `test_revert_executePvP_replaySameBundle`: execute a valid bundle, then re-submit identical calldata → WrongRoundNonce bubbles (structural replay protection, RESEARCH Q2 — no router state needed)
    - `test_singleLegDirectSubmissionSettles`: build a full valid PvP bundle, then call `hubUSDC.executeRound` DIRECTLY with the USDC leg's calldata and signatures — assert it SETTLES (nonce advances, deltas applied), then assert the router bundle now reverts WrongRoundNonce. Header comment must state the accept-and-document disposition verbatim in spirit: leg consents are valid standalone hub signatures; the downgrade is to ordinary collateral-backed credit risk, never unsigned movement; reference docs/THREAT-MODEL.md (RESEARCH Q2c — this test is the machine documentation D-13 cites).
    All reverts asserted via `vm.expectRevert(abi.encodeWithSelector(...))` — never string matching.
  </action>
  <verify>
    <automated>cd contracts && forge test --match-contract PvPRouterTest -vvv && forge test --match-test test_singleLegDirectSubmissionSettles -vvv</automated>
  </verify>
  <acceptance_criteria>
    - All ten matrix tests pass; each revert test contains ≥4 post-revert assertions (2 nonces + ≥2 collateral reads)
    - `grep -c 'test_singleLegDirectSubmissionSettles' contracts/test/PvPRouter.t.sol` outputs ≥1 and the test asserts the leg SETTLED (positive assertion), not that it reverted
    - `grep -v '^\s*//' contracts/test/PvPRouter.t.sol | grep -c 'expectRevert("'` outputs 0 (no string-revert assertions)
    - `npm run test:contracts` exits 0
  </acceptance_criteria>
  <done>Both-or-neither holds against every identified failure mode, and the single accepted limitation is machine-documented exactly as the threat model will describe it.</done>
</task>

<task type="auto">
  <name>Task 3: Gas measurement + snapshot + abi module + PvPRouterClient</name>
  <files>contracts/test/PvPRouter.t.sol, contracts/.gas-snapshot, src/abi/PvPRouter.ts, src/client.ts</files>
  <read_first>contracts/test/ClearingHubV2.t.sol lines 406-439 (gasleft() measurement idiom + console2.log), src/client.ts lines 18-37 (coefficient provenance comments), 238-263 (executeRound write shape), src/abi/ClearingHubV2.ts lines 1-10 (module shape: abi const + bytecode const), 04-RESEARCH.md Q6 (formula shape + expected magnitudes)</read_first>
  <action>
    Gas tests (gasleft() deltas around fresh-state worst-case executePvP calls, console2.log one line per size): `test_gas_executePvP_small` (two legs n=3 each, m=10 each, union per _simplePvP) and `test_gas_executePvP_demoScale` (n=5 per leg, m=105 per leg, union=5 — mirrors the Phase 2 measured points). Append the measured lines to contracts/.gas-snapshot following its existing format.

    Create src/abi/PvPRouter.ts mirroring src/abi/ClearingHubV2.ts exactly: `pvpRouterAbi` and `pvpRouterBytecode` extracted from `contracts/out/PvPRouter.sol/PvPRouter.json` after `forge build` (same extraction method the existing module used).

    Extend src/client.ts: constants `PVP_ROUTER_GAS_BASE` and `PVP_GAS_PER_UNION_SIG` fitted from the two measured points with ≥1.5x margin, each with a provenance comment naming this plan, the date, and the measured numbers (house style at client.ts:20-29). New `PvPRouterClient` class (HubClient shape: constructor(router: Address, rpcUrl?), private pub): reads `hubUSDC()`, `hubEURC()`, `hashPvPRound(...)` via readContract; write `executePvP(wallet: WalletClient, proposal: PvPProposal, legSignatures: { usdc: Hex[]; eurc: Hex[] }, pvpSignatures: Hex[]): Promise<Hex>` assembling both Leg tuples from the embedded RoundProposals, passing the signed leg digests explicitly, with `gas = PVP_ROUTER_GAS_BASE + 2n*EXECUTE_ROUND_GAS_BASE + EXECUTE_ROUND_GAS_PER_PARTICIPANT*BigInt(n1+n2) + EXECUTE_ROUND_GAS_PER_ID*BigInt(m1+m2) + PVP_GAS_PER_UNION_SIG*BigInt(nUnion)` (nUnion via unionParticipants from src/pvp.ts), `maxFeePerGas: MIN_MAX_FEE_PER_GAS` — never gas estimation (Arc gas-token gotcha). Export the class and constants; add NatSpec-style doc comments.
  </action>
  <verify>
    <automated>cd contracts && forge test --match-test test_gas_executePvP -vvv && cd .. && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Both gas tests pass and print measured values via console2.log; contracts/.gas-snapshot contains ≥2 new executePvP lines
    - `PVP_ROUTER_GAS_BASE`/`PVP_GAS_PER_UNION_SIG` provenance comments cite the exact measured numbers; formula for the demo-scale point yields ≥1.5x the measured gas (arithmetic shown in the comment)
    - `grep -c 'estimateGas\|estimateContractGas' src/client.ts` outputs 0
    - `npx tsc --noEmit` and `npm test` exit 0
  </acceptance_criteria>
  <done>executePvP has measured gas, a formula-driven client with explicit limits, and an abi module ready for the demo bootstrap (plan 04-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adversarial submitter → router | Matrix tests model every calldata manipulation an attacker can attempt |
| leg-signature holder → hubs directly | test_singleLegDirectSubmissionSettles models the one path the router cannot close |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Tampering | atomicity under any leg failure | mitigate | 8-way revert matrix with post-revert state assertions on BOTH hubs |
| T-04-16 | DoS/Tampering | single-leg extraction | accept | machine-documented by test_singleLegDirectSubmissionSettles; downgrade bounded to ordinary credit risk (RESEARCH Q2c); prose lands in plan 04-07 THREAT-MODEL rows |
| T-04-17 | Replay | full-bundle re-submission | mitigate | structural (leg roundNonce) — proven by test_revert_executePvP_replaySameBundle, no router state |
| T-04-18 | DoS | gas underestimation on Arc | mitigate | measured constants + ≥1.5x-margin formula + explicit gas on every write, never estimation |
| T-04-SC | Tampering | package installs | accept | no new packages this phase |
</threat_model>

<verification>
- `npm run test:contracts` green (smoke + positive + matrix + parity + gas)
- `npm test && npx tsc --noEmit` green
</verification>

<success_criteria>
- PVP-01 proven both positively and against every enumerated failure mode, at the contract level
- The accepted limitation is a passing test, not a footnote
- Client can submit a PvP round on Arc with a justified explicit gas limit
</success_criteria>

<output>
Create `.planning/phases/04-cross-currency-pvp-rounds-brief-phase-6/04-04-SUMMARY.md` when done.
</output>
