---
phase: 04-cross-currency-pvp-rounds-brief-phase-6
plan: 06
type: execute
wave: 4
depends_on: [04-05]
files_modified:
  - demo/e2e.ts
  - public/dashboard.html
autonomous: true
requirements: [PVP-01]
tags: [e2e, anvil, dashboard, both-or-neither]

must_haves:
  truths:
    - "e2e proves both-or-neither POSITIVELY on anvil: both legs settle atomically, per-persona balances exact per the FX rate pairs, measured gasUsed printed (D-11, A3 record)"
    - "e2e proves both-or-neither NEGATIVELY: a sabotaged leg means NEITHER settles — asserted from CHAIN state (both nonces, all collateral on both hubs), plus a forced on-chain revert variant with a mined status:reverted receipt (Pitfall 7)"
    - "Dashboard shows at most a PvP badge on PvP rounds — no new panels or endpoints (D-12)"
  artifacts:
    - path: "demo/e2e.ts"
      provides: "PvP positive + negative scenarios appended to the existing run"
      contains: "gasUsed"
    - path: "public/dashboard.html"
      provides: "PvP badge cell in the rounds table"
      contains: "pvp"
  key_links:
    - from: "demo/e2e.ts"
      to: "demo/pvp.ts"
      via: "runPvPRound + fxTradePair"
      pattern: "runPvPRound"
    - from: "public/dashboard.html"
      to: "/state rounds serialization"
      via: "r.pvp field from ExecutedRound"
      pattern: "\\.pvp"
---

<objective>
Prove D-11 on anvil: extend `demo/e2e.ts` with the PvP scenario — positive (atomic settlement, FX-exact balances, gasUsed recorded) and negative (sabotaged leg → neither settles, asserted on-chain) — and add the D-12-bounded dashboard PvP badge.

Purpose: this is the phase's executable success criterion — "both settle or neither does", demonstrated, not asserted.
Output: `npm run e2e:anvil` covering PvP both ways; minimal dashboard surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-cross-currency-pvp-rounds-brief-phase-6/04-CONTEXT.md
@.planning/phases/04-cross-currency-pvp-rounds-brief-phase-6/04-RESEARCH.md

<interfaces>
From demo/pvp.ts (plan 04-05): `runPvPRound(deps)`, `attemptPvPRound(args)`, `fxTradePair(a, b, usdcAmount, quote, opts)`.
From demo/fx.ts (plan 04-05): `sampleQuote()`, `quoteToRate(q)`.
From demo/setup.ts (plan 04-05): `DemoEnv` with `hubEurc`, `hubClientEurc`, `router`, `routerClient`, `tokenEurc`.
From src/client.ts (plan 04-04): `PvPRouterClient.executePvP(wallet, proposal, legSignatures, pvpSignatures)` — callable directly for the forced-revert variant.
From demo/e2e.ts (existing): `check(cond, label)`, `snapshot()`, `assertDeltas(before, deltas, label)`, receipt-status pattern, `env.anvil?.kill()` exit hygiene (WR-08).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e PvP positive scenario — atomic settlement with FX-exact balances</name>
  <files>demo/e2e.ts</files>
  <read_first>demo/e2e.ts (whole file — check/snapshot/assertDeltas at 29-81, liveness scenario structure at 183-204, receipt pattern at 313-314, exit hygiene at 104-109, bytecode-tail guard at 42-50), 04-RESEARCH.md Pitfall 7 and Q6 (gasUsed record for A3)</read_first>
  <action>
    Append a PvP scenario to the anvil e2e run: seed 2-3 FX trade pairs between the designated FX personas via `fxTradePair` at the `sampleQuote()` rate, plus a few ordinary same-currency IOUs on EACH hub so both legs mix FX and non-FX flows (this exercises the Pitfall-6-safe design: rate checks are per pair, not per delta). Snapshot per-persona collateral on BOTH hubs (per-hub snapshot variants), compute expected deltas by running net() locally per hub, then run `runPvPRound`. Assert with check(): both hub roundNonces advanced exactly 1; per-persona on-chain collateral deltas equal the engine deltas to the base unit on BOTH hubs (assertDeltas per hub); the router tx receipt status is success; print the receipt's `gasUsed` with a `[e2e] pvp gasUsed=` line (the A3 record — Arc block gas limit unverified, this number is the evidence). Add the optional bytecode-tail guard for the router (metadata-tail compare against the local artifact, existing idiom). Preserve exit hygiene: `env.anvil?.kill()` on every failure path.
  </action>
  <verify>
    <automated>npm run e2e:anvil</automated>
  </verify>
  <acceptance_criteria>
    - `npm run e2e:anvil` exits 0; output contains `pvp gasUsed=` followed by a number
    - Positive assertions cover: 2 nonce checks + per-persona delta checks on both hubs + receipt success (≥6 new check() lines)
    - Both legs contain at least one non-FX IOU (mixed flows, visible in the seeded IOU construction)
    - All pre-existing e2e scenarios still pass unchanged
  </acceptance_criteria>
  <done>Atomic cross-currency settlement is demonstrated end-to-end on anvil with FX-exact balance math and a recorded gas figure.</done>
</task>

<task type="auto">
  <name>Task 2: e2e negative scenarios + dashboard PvP badge</name>
  <files>demo/e2e.ts, public/dashboard.html</files>
  <read_first>demo/e2e.ts liveness/negative-scenario structure, public/dashboard.html lines 19, 59-60 (badge CSS), 147-161 (rounds-table row renderer), 04-RESEARCH.md Pitfall 7 (vacuous-pass trap)</read_first>
  <action>
    Negative variant (a) — consent sabotage (D-11 "withhold one consent"): configure an FX-trader persona to refuse in BOTH passes (reuse/extend the stalled-flag mechanics so pass-2 also fails — a persistent refusal, since a single pass-1 stall would just get them excluded and the round would settle without them, which is the liveness feature, not the sabotage). Run the PvP attempt; assert outcome is aborted AND from chain state: both roundNonces unchanged, every persona's collateral unchanged on both hubs (fresh snapshots compared map-equal — never coordinator state alone, Pitfall 7).

    Negative variant (b) — forced on-chain revert: build a valid bundle via attemptPvPRound with a recording submit that captures the proposal and signatures WITHOUT broadcasting, corrupt one EURC leg signature in the captured set, then call `routerClient.executePvP` directly with the corrupted set. Assert: the tx mines with receipt `status: "reverted"`; both roundNonces unchanged; all collateral unchanged on both hubs. (This proves revert-bubbling atomicity on a real node, complementing the forge matrix.)

    public/dashboard.html: in the rounds-table row renderer, when `r.pvp` is present render a PvP badge cell using the existing badge CSS, with the fx rate shown as `num/den` text in the cell title. No new panels, no new endpoints, no new polling — D-12 bound.
  </action>
  <verify>
    <automated>npm run e2e:anvil && grep -v '^\s*<!--' public/dashboard.html | grep -ci 'pvp'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run e2e:anvil` exits 0 including both negative variants
    - Variant (a) asserts nonces + full collateral maps unchanged on BOTH hubs (on-chain reads, ≥4 check() lines)
    - Variant (b) asserts receipt status === "reverted" plus unchanged nonces/collateral (≥4 check() lines)
    - `grep -v '^\s*<!--' public/dashboard.html | grep -ci 'pvp'` ≥ 1; `git diff demo/server.ts` shows no endpoint additions
  </acceptance_criteria>
  <done>Neither-settles is proven both at the consent layer and against a real reverted transaction, and the dashboard surface stays minimal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e assertions → chain | Negative proofs must come from chain state; coordinator-state-only assertions pass vacuously (Pitfall 7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-24 | Tampering | vacuous negative test | mitigate | on-chain nonce + collateral assertions on both hubs; forced-revert variant requires a mined reverted receipt |
| T-04-25 | Tampering | half-settle on a live node | mitigate | variant (b) demonstrates revert bubbling on anvil, not just in forge |
| T-04-26 | Information disclosure | dashboard scope creep | mitigate | D-12 grep gate: badge only, no new endpoints |
| T-04-SC | Tampering | package installs | accept | no new packages this phase |
</threat_model>

<verification>
- `npm test && npm run test:contracts && npm run e2e:anvil` all green (the phase-gate full suite)
</verification>

<success_criteria>
- PVP-01's success criterion is executable: both-or-neither shown positively and negatively on anvil with FX-exact balances (D-11)
- gasUsed recorded for the A3 assumption log
</success_criteria>

<output>
Create `.planning/phases/04-cross-currency-pvp-rounds-brief-phase-6/04-06-SUMMARY.md` when done.
</output>
