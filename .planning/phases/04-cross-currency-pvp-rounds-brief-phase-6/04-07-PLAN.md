---
phase: 04-cross-currency-pvp-rounds-brief-phase-6
plan: 07
type: execute
wave: 5
depends_on: [04-06]
files_modified:
  - docs/PROTOCOL.md
  - docs/THREAT-MODEL.md
  - docs/CONCEPTS.md
  - README.md
  - contracts/script/DeployPvPRouter.s.sol
autonomous: false
requirements: [PVP-01, PVP-02]
tags: [docs, deploy, testnet, threat-model, human-verify]

must_haves:
  truths:
    - "PROTOCOL.md documents the PvPRound struct (exact typehash), the atomicity argument, the num/den + shared-ref rate semantics, the arc-stablecoin-fx tie-in with the D-06 fallback stated, and the third superseded non-goal note"
    - "THREAT-MODEL.md states the single-leg-extraction limitation PLAINLY (accept-and-document, Herstatt-style downgrade to ordinary credit risk, never unsigned movement) and that the FX rate is agreed, not oracle-derived"
    - "PvPRouter is live on Arc testnet, source-verified, bound to the existing V2 hubs, and recorded in README"
    - "A human has walked through the live router and the e2e run and approved"
  artifacts:
    - path: "contracts/script/DeployPvPRouter.s.sol"
      provides: "DeployV2-pattern deploy script reading HUB_V2_USDC/HUB_V2_EURC"
      contains: "HUB_V2_EURC"
    - path: "docs/PROTOCOL.md"
      provides: "PvP section + measured-gas rows + superseded non-goal note"
      contains: "PvPRound(bytes32 usdcLegDigest,bytes32 eurcLegDigest,uint256 fxNumerator,uint256 fxDenominator)"
    - path: "docs/THREAT-MODEL.md"
      provides: "PvP attack-surface rows + known-limitation row"
      contains: "single-leg"
    - path: "README.md"
      provides: "router address row + CLS positioning one-liner"
      contains: "PvPRouter"
  key_links:
    - from: "docs/THREAT-MODEL.md"
      to: "contracts/test/PvPRouter.t.sol"
      via: "citation of test_singleLegDirectSubmissionSettles as machine documentation"
      pattern: "test_singleLegDirectSubmissionSettles"
    - from: "docs/PROTOCOL.md"
      to: "contracts/.gas-snapshot"
      via: "measured executePvP gas rows"
      pattern: "executePvP"
---

<objective>
Close the phase honestly (D-13, D-10): PROTOCOL/THREAT-MODEL/README/CONCEPTS documentation with the atomicity argument and the plainly stated single-leg limitation, the Arc testnet router deployment with explicit gas and source verification, and the final human-verify walkthrough.

Purpose: the primitive's submission surface is complete only when the docs are as honest as the tests and the router is live where reviewers can see it.
Output: docs, deploy script, live verified router, human approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-cross-currency-pvp-rounds-brief-phase-6/04-CONTEXT.md
@.planning/phases/04-cross-currency-pvp-rounds-brief-phase-6/04-RESEARCH.md

<interfaces>
Canonical typehash (must appear verbatim in PROTOCOL.md):
```
PvPRound(bytes32 usdcLegDigest,bytes32 eurcLegDigest,uint256 fxNumerator,uint256 fxDenominator)
```
Rate convention to pin: fxNumerator = EURC base units, fxDenominator = USDC base units; a pairing of u USDC with e EURC is consistent iff `e * fxDenominator == u * fxNumerator`; checked per shared-`ref` IOU pair, NEVER per net delta.

From contracts/script/DeployV2.s.sol (existing): the vm.envAddress + startBroadcast + console.log pattern and the file-header invocation comment with `--with-gas-price 25gwei`.

THREAT-MODEL limitation framing (RESEARCH Q2c, use this in substance): "PvP both-or-neither holds within the router path and against all failure/revert modes; it does not hold against an adversary who obtains and unilaterally submits one leg's full signature set (including mempool extraction of the router tx). The downgrade is to standard netting credit risk, never to unsigned movement."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PROTOCOL, THREAT-MODEL, CONCEPTS, README prose</name>
  <files>docs/PROTOCOL.md, docs/THREAT-MODEL.md, docs/CONCEPTS.md, README.md</files>
  <read_first>docs/PROTOCOL.md (Messages/EIP-712 section shape at ~line 35, measured-gas table at ~510, superseded-non-goal notes at 528-538), docs/THREAT-MODEL.md (attack-surface row format at ~29, known-limitations rows 18/19 phrasing at 52-59), README.md lines 175-204 (deployed-hubs table), docs/CONCEPTS.md (tone for the one-liner), 04-RESEARCH.md Patterns 1/2/4 + Q2c</read_first>
  <action>
    docs/PROTOCOL.md — add a "Cross-currency PvP rounds" section after IOU redemption, following the existing section shapes: (1) PvPRound struct with the verbatim typehash literal + field table + domain ("ArclearPvPRouter", "1", router as verifyingContract); (2) atomicity argument: one router tx, plain external calls, revert bubbling is the mechanism, no try/catch by construction; hub pair pinned as constructor immutables; (3) rate semantics: the num/den convention from the interfaces block, the shared-`ref` FX-pair rule, and the explicit warning that net deltas are NOT rate-checkable (mixed flows); (4) arc-stablecoin-fx tie-in paragraph: per-round rate agreed in the same amount-pair form the official sample's App Kit quotes use; a production coordinator would source the pair from an estimateSwap quote; the shape-mirror is the D-06-sanctioned fallback — say so explicitly; (5) standing-consent note: no deadline field by design, the legs are the replayable objects, identical to the base protocol's documented property; (6) add executePvP rows to the measured-gas table from contracts/.gas-snapshot; (7) add the third superseded note under Explicit non-goals for "No cross-currency rounds (one hub = one token)", matching the existing superseded-note style.

    docs/THREAT-MODEL.md — attack-surface rows citing their tests: single-leg extraction (accept-and-document; cite test_singleLegDirectSubmissionSettles; include the Q2c framing sentence from the interfaces block in substance), evil-hub substitution (constructor immutables; no calldata hub addresses exist), leg/digest mismatch (LegDigestMismatch + its test), PvPRound replay (structural via leg roundNonces + replay test, no router state), FX-rate manipulation (bounded by unanimous consent; state PLAINLY that the rate is agreed, not oracle-derived — CONTEXT specifics), signature malleability (OZ ECDSA). Known-limitations row for the mempool-extraction residual with the Herstatt-style credit-risk downgrade framing and the coordinator signature-custody rule (leg signatures published only inside the router tx).

    README.md — one-sentence positioning tie-in near the existing CLS/netting narrative: netting compresses obligations within a token; PvP composes two hubs atomically across tokens (miniature CLS). docs/CONCEPTS.md — the matching one-liner in its voice. (The router address table row lands in Task 2 after deployment.)
  </action>
  <verify>
    <automated>grep -c 'PvPRound(bytes32 usdcLegDigest,bytes32 eurcLegDigest,uint256 fxNumerator,uint256 fxDenominator)' docs/PROTOCOL.md && grep -ci 'oracle' docs/THREAT-MODEL.md && grep -c 'test_singleLegDirectSubmissionSettles' docs/THREAT-MODEL.md</automated>
  </verify>
  <acceptance_criteria>
    - Typehash literal appears verbatim in docs/PROTOCOL.md (grep count ≥ 1)
    - docs/THREAT-MODEL.md contains the phrase "agreed, not oracle-derived" (or byte-equivalent) and cites test_singleLegDirectSubmissionSettles
    - docs/PROTOCOL.md Explicit non-goals section contains a third "Superseded:" note mentioning cross-currency
    - docs/PROTOCOL.md measured-gas table gained ≥2 executePvP rows whose numbers match contracts/.gas-snapshot
    - README.md and docs/CONCEPTS.md each gained exactly one PvP positioning sentence (CLS analogy present)
  </acceptance_criteria>
  <done>The docs state what the system guarantees, what it accepts, and why — with the limitation framed exactly as the research bounded it (D-13).</done>
</task>

<task type="auto">
  <name>Task 2: DeployPvPRouter.s.sol + Arc testnet deployment + verification + README record</name>
  <files>contracts/script/DeployPvPRouter.s.sol, README.md</files>
  <read_first>contracts/script/DeployV2.s.sol (whole file — env reads, broadcast block, console.log, header invocation comment), README.md deployed-hubs table (175-204) for the row format and the current HUB_V2_USDC/HUB_V2_EURC addresses</read_first>
  <action>
    Create contracts/script/DeployPvPRouter.s.sol on the DeployV2 pattern: read `HUB_V2_USDC` and `HUB_V2_EURC` via vm.envAddress, vm.startBroadcast, `new PvPRouter(ClearingHubV2(hubUsdc), ClearingHubV2(hubEurc))`, vm.stopBroadcast, console.log the router address and both hub bindings. File-header comment documents the exact invocation including `--rpc-url arc_testnet --broadcast --with-gas-price 25gwei` (explicit gas mandatory — USDC is Arc's gas token) and the verify step.

    Execute the deployment against Arc testnet (env from .env: ARC_RPC_URL, DEPLOYER_PK, HUB_V2_USDC, HUB_V2_EURC — if credentials are missing or unfunded, raise an authentication checkpoint rather than skipping; D-10 requires the live deploy). Verify the source on the Arc explorer (Blockscout verify flow used for the V2 hubs; same compiler settings, constructor args ABI-encoded). Confirm the deployment with cast: `hubUSDC()` and `hubEURC()` reads must return the README's recorded V2 hub addresses. Add the router row to README's deployed table in the linked-address + "source verified" format, and record the address as the `PVP_ROUTER` documentation value.
  </action>
  <verify>
    <automated>source .env && cast call $(grep -oE '0x[a-fA-F0-9]{40}' README.md | tail -1) "hubUSDC()(address)" --rpc-url "$ARC_RPC_URL"</automated>
  </verify>
  <acceptance_criteria>
    - `forge script contracts/script/DeployPvPRouter.s.sol` broadcast succeeds on Arc testnet (chain id 5042002) with explicit gas price
    - `cast call <router> "hubUSDC()(address)"` and `"hubEURC()(address)"` against ARC_RPC_URL return exactly the README's HUB_V2_USDC / HUB_V2_EURC addresses
    - Router source shows as verified on testnet.arcscan.app
    - README deployed table contains the router row with a working arcscan link and "source verified ✓"
  </acceptance_criteria>
  <done>PvPRouter is live on Arc testnet, cryptographically bound to the existing V2 hubs, source-verified, and recorded (D-10).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final walkthrough — live router + e2e + docs honesty</name>
  <files>none — verification only</files>
  <action>Present the walkthrough below to the developer and BLOCK until they respond. Do not mark the phase complete without an explicit "approved". This checkpoint verifies work Claude already automated (deploy, tests, e2e, docs) — it performs no new work.</action>
  <what-built>The complete Phase 4 PvP stack: PvPRouter live and verified on Arc testnet bound to the existing V2 hubs; SDK PvP consent layer with TS↔Solidity digest parity; full forge matrix including the machine-documented single-leg limitation; dual-hub demo with e2e both-or-neither proof; PROTOCOL/THREAT-MODEL/README/CONCEPTS documentation.</what-built>
  <how-to-verify>
    1. Open the router on https://testnet.arcscan.app/address/<router-address-from-README> — confirm "source verified" and that the contract tab shows hubUSDC/hubEURC matching the README hub table.
    2. Run `npm test && npm run test:contracts` — everything green, including PvPParityTest and test_singleLegDirectSubmissionSettles.
    3. Run `npm run e2e:anvil` — watch the PvP scenario: positive settle with the `pvp gasUsed=` line, then both negative variants (aborted bundle, reverted forced submission) proving neither settles.
    4. Optionally run `npm run demo -- --anvil`, trigger activity, and confirm the dashboard shows the PvP badge on the PvP round and nothing else changed.
    5. Read the PROTOCOL.md PvP section and the THREAT-MODEL.md rows: confirm the single-leg limitation is stated plainly (downgrade to ordinary credit risk, never unsigned movement) and the rate is described as agreed, not oracle-derived.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 4, or describe issues to fix</resume-signal>
  <verify>Developer replies "approved" (or issues are fixed and re-verified)</verify>
  <acceptance_criteria>Explicit human approval recorded in the execution log; any reported issue resolved before phase close</acceptance_criteria>
  <done>Phase 4 approved by the developer against the live testnet router, the green full suite, and the honesty review of the docs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy credentials → chain | DEPLOYER_PK signs a real testnet deployment; explicit gas prevents estimation reserving the gas-token balance |
| docs → reviewers | The threat model is a trust interface: overstating guarantees is the threat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-27 | Spoofing | wrong hub binding at deploy | mitigate | post-deploy cast reads of hubUSDC()/hubEURC() must equal README's recorded V2 addresses before the README row is written |
| T-04-28 | Repudiation | unverifiable deployed source | mitigate | Blockscout source verification + README "source verified" convention + human checkpoint eyeballs it |
| T-04-29 | Information disclosure | overclaiming atomicity in docs | mitigate | THREAT-MODEL states the single-leg residual verbatim-in-substance from Q2c; human checkpoint reviews honesty explicitly |
| T-04-30 | DoS | deploy gas estimation on Arc | mitigate | --with-gas-price 25gwei explicit, per the documented Arc gas-token gotcha |
| T-04-SC | Tampering | package installs | accept | no new packages this phase |
</threat_model>

<verification>
- Full suite green: `npm test && npm run test:contracts && npm run e2e:anvil`
- Live router verified on arcscan with correct immutable bindings
- Human approval recorded
</verification>

<success_criteria>
- D-10 and D-13 delivered; PVP-01/PVP-02 complete end-to-end from contract to live deployment to honest documentation
- Phase 4 (the final roadmap phase) closes with human sign-off
</success_criteria>

<output>
Create `.planning/phases/04-cross-currency-pvp-rounds-brief-phase-6/04-07-SUMMARY.md` when done.
</output>
